# Estado Atual — CRM Vértice Digital

Última atualização: 10/08/2026
Versão: v2.36.14

## Build
- TypeScript: 0 erros (tsc --noEmit, 09/06/2026)
- Build vite: limpo (✓ 6.53s; só aviso pré-existente de bundle >500KB)
- tsconfig fix (09/06/2026): removidos `baseUrl: "."` + `ignoreDeprecations: "6.0"` (TS 5.9.3 rejeita "6.0"); paths ajustado para `"./src/*"`. tsc --noEmit agora funciona.
- Fix v2.33.7 type-safety (09/06/2026): zerados os 19 erros de tipo. (1) `reciboSlice.formatRecibo` retipado `Record<string,unknown>`→`any` (padrão dos demais formatters) + hardening leve: `status` valida contra `VALID_RECIBO_STATUS` com fallback `'pendente'`, `itens` via `Array.isArray` (15 erros). (2) `financeiroSlice.ts:45` guard `!!data.tipo &&` antes do `.includes()` — `data.tipo` é `string|undefined` em `Partial<Transaction>` (NÃO era risco cross-tenant; workspaceId já tinha early-return). (3) Dead code: `IAAssistente.tsx:211` callback `h` anotado `HistoricoEntry`; `Recibos.tsx` import `Receipt` removido; `whatsappSlice.ts` const `jidToNumber` removida (morta desde v2.32.2).

## Últimas Mudanças
- **UX v2.36.14 (WorkspaceSwitcher: linha 2 do header mobile vira gatilho compacto + bottom sheet):** mudança **só de apresentação mobile** — nenhuma linha de lógica de troca, persistência, autorização, carregamento de stores ou realtime foi tocada. **Motivação:** o header mobile carregava uma **linha 2 dedicada** (`MainLayout.tsx:127-130`, `md:hidden px-3 pb-2`) só pra segurar um botão de workspace de largura total, custando ~52px de altura em toda tela; e o `pb-2` **ocupava espaço mesmo com 1 workspace só** (o badge `!multi` é `hidden md:flex`, invisível dentro de um pai `md:hidden`). Pior, o dropdown mobile (herdado do `14c9cdd4`) era um `position:absolute` sem backdrop e sem scroll-lock, fechando **só** por `mousedown` fora — e `mousedown` não dispara de forma confiável antes do `touchend` no iOS, então "tocar fora pra fechar" era instável no celular. **Achado da investigação — não existe bottom sheet reutilizável no projeto:** `src/components/ui/` tem só `Modal` + `SlidePanel` (mesmo arquivo), ambos centrados/laterais; os modais de formulário ("Novo Orçamento" `Orcamentos.tsx:527`, "Novo Recibo" `Recibos.tsx:371`) usam o `Modal` **centrado**, idêntico em desktop e celular — nada sobe de baixo em lugar nenhum; zero `createPortal` em `src/`; zero libs de animação/gesto (sem framer-motion, vaul, radix, headless). **Mas o `SlidePanel` é o molde certo** — sempre montado + `transform transition-transform duration-200` + `translate-x-full → translate-x-0` é literalmente o padrão de sheet, só que no eixo X; rotacionar pra Y foi reúso de padrão real, não um segundo padrão. **Arquivos (4):** (1) `Modal.tsx` ganhou um **terceiro export `BottomSheet`**, aditivo — `Modal` e `SlidePanel` ficaram byte-idênticos, e o novo componente reaproveita os imports que já estavam no arquivo (`ReactNode`/`useEffect`/`X`/`cn`) e o mesmo bloco de scroll-lock `document.body.style.overflow` (4ª cópia; as 3 existentes em `Modal.tsx:16`, `Modal.tsx:77` e `Sidebar.tsx:94` **não** foram refatoradas — fora de escopo). Painel `fixed bottom-0 inset-x-0 z-[201] rounded-t-[var(--radius-xl)] max-h-[85vh]` com `translate-y-full → translate-y-0`, backdrop `z-[200]`, grabber 36×4px em botão de 44px que fecha, X de 44×44, corpo com `paddingBottom: env(safe-area-inset-bottom, 0px)`, `Escape` pra fechar e `role="dialog"`/`aria-modal` (o `Modal`/`SlidePanel` não têm nenhum dos dois; por ser componente novo, não afeta ninguém). z-index 200/201 escolhido pra ficar acima dos dropdowns de 200 (`WorkspaceSwitcher`, `NotificationsDropdown`) e do `BottomNav` (item de fluxo sem z-index — mesma lógica de pintura do fix `195fcb9b` da Sidebar). `--shadow-modal` usado de propósito: `index.css:23-67` redefine `:root` **depois** do `@import` de tokens.css, então `--shadow-lg` está sobrescrito com valor light-theme e `--radius-xl` vale **10px** em runtime, não os 12px de tokens.css. (2) `WorkspaceSwitcher.tsx` ganhou prop `variant?: 'desktop' | 'mobile'` (default `'desktop'`) — o `useEffect` de fetch, `handleSelect`, `ACTIVE_WORKSPACE_KEY`, `activeNome`/`multi`, o badge `!multi` e **todo o bloco do popover desktop** ficaram intocados; o ramo mobile novo entra antes do badge e retorna `null` quando `!multi`. Único ajuste no caminho compartilhado: o listener de clique-fora ganhou `variant !== 'desktop'` no early-return (no mobile quem fecha é backdrop/X/Esc), o que não altera o desktop. Linhas do sheet com `min-h-[56px]`, check `var(--accent)` e label "Empresa atual" sob a ativa; lista sai de uma const `visible` pra que um filtro/busca futuro seja **uma linha** (busca **não** adicionada agora — poucas empresas). (3) `HeaderGlobal.tsx` recebeu `<WorkspaceSwitcher variant="mobile" />` num wrapper `md:hidden min-w-0 -ml-1` **abaixo do `<h1>`**, ocupando no mobile o espaço onde data/subtítulo já eram `hidden md:block`; a linha do `<h1>` ficou byte-idêntica e a instância desktop (`:78-80`) não foi tocada. (4) `MainLayout.tsx` perdeu o bloco da linha 2 e o import órfão; o wrapper `flex flex-col` foi mantido (carrega `bg-[var(--bg-sidebar)]` + `border-b`). **Autorização (trava confirmada):** a regra segue sendo **exclusivamente** `multi = workspaces.length >= 2`, derivada de `workspace_members` filtrada por `user_id`; gatilho **e** sheet vivem dentro desse ramo, então quem tem 1 workspace não monta nenhum dos dois — no mobile continua vendo nada, exatamente como antes. **Drag-to-dismiss: deliberadamente NÃO implementado.** A infra não suporta sem custo — sem lib de gesto e sem precedente de touch-drag no projeto (os únicos `onTouchStart/Move` são o canvas de `SignaturePad.tsx:195`); exigiria ~25 linhas de state de `deltaY` + desligar a `transition` durante o arraste. O grabber ficou como aforância visual **e** área de toque que fecha. **Correção de rota durante a verificação (o motivo de a medição existir):** a primeira implementação pôs o gatilho **inline ao lado do título**, opção escolhida sob a minha estimativa de que só truncaria em 320px. A medição provou o contrário e a decisão foi revertida — sobram ~190px pro bloco esquerdo no mobile (hambúrguer 48 + sino 44 + avatar 48 + gaps consomem o resto), contra 113px de título + 8 de gap + 86 do gatilho = **207px**, então **título e nome da empresa truncavam juntos em 360px E 390px** (h1 88px/105px, gatilho 64px/77px) — reintroduzindo exatamente o bug "Da..." que o v2.36.5 tinha consertado. Empilhado custa 15px a mais de header e resolve os dois. **Verificação:** `tsc --noEmit` 0 erros; `vite build` limpo (só o aviso pré-existente de bundle >500KB); as 8 classes novas confirmadas no CSS compilado (atenção: o Tailwind v4 escapa `(`, `)` e `%` no seletor — `rounded-t-\[var\(--radius-xl\)\]`, `max-w-\[45\%\]` — um grep sem isso dá falso negativo). Medido em **Puppeteer headless** com o CSS **realmente compilado** (fixture isolado replicando `MainLayout` + `HeaderGlobal` + `BottomNav`, lado "before" com o markup da linha 2), em **4 combinações** de título × empresa ("Dashboard Executivo"/"Comparador de Preços" × "FL Art Metal"/"GPP Móveis Planejados") × 3 viewports: **header mobile 133px → 96px** (−37px) em 360×640 e 390×844; **zero truncamento** de título e de empresa nas 4 combinações (inclusive "GPP Móveis Planejados", gatilho de 137px), contra o inline que truncava os dois; gatilho **83–137 × 44px** (regra 9); `docScrollWidth == clientWidth` em tudo. Sheet: fechado fora da viewport (`y = altura da tela`), aberto em y=422 @640 e y=626 @844 com backdrop cobrindo 360×640 / 390×844 inteiros, linhas de **60px e 56px**, X 44×44, grabber 44px, `body.overflow: hidden`, e `elementFromPoint` no meio do `BottomNav` acertando o **conteúdo do sheet** — ou seja, o nav fica coberto e não recebe toque. **Desktop 1280×800: header 93px nos dois lados, h1 sem truncar com a mesma largura (134px), `#desktop-switcher` em `display:flex` e o gatilho mobile em `0×0` (display:none)** — desktop provado intocado. **Pendência:** confirmação visual sua em celular real (360/390) com conta de 2+ workspaces — as medições cobrem geometria, não percepção; e um teste com conta de 1 workspace só pra confirmar que nada aparece no mobile. **Registrado e não corrigido (fora de escopo, comportamento idêntico ao de hoje):** o `WorkspaceSwitcher` segue montado 2× (desktop + mobile), rodando as 3 queries de fetch em duplicata a cada load — dedupe via cache module-level da promise fica como follow-up.
- **Fix v2.36.13 (Orçamentos: card e header estouravam na horizontal no mobile — botões PDF/PIX fora da tela):** reportado em celular real logo após o v2.36.12. É **exatamente a pendência nº 2 daquele fix**: sem `overflow-y-auto`, o `overflow-x` da lista voltou a `visible` e expôs um estouro que já existia latente, antes clipado. Só `Orcamentos.tsx` tocado. **Causa (aritmética, medida):** largura útil real num 360px = `360 − 32` (`p-4` do `<main>`) `− 48` (`p-6` da lista) `− 32` (`p-4` do Card) = **248px**; a linha do card era `flex items-center gap-4` **sem `flex-wrap`** com 3–4 filhos que não encolhem — bloco de conteúdo `flex-1` **sem `min-w-0`** (min-content ~185px, travado pela linha de meta `flex gap-4` sem wrap com `orc.numero` em `font-mono`, inquebrável), bloco de valor `text-right` (~105px, `formatCurrency` inquebrável), bloco de ações `flex gap-1` sem wrap (~126px, porque abaixo de 640px os botões mostram ícone **+ rótulo** "PDF"/"PIX" via `sm:hidden`) e 32px de gaps ≈ **448px contra 248px**. O header ([:403](src/modules/orcamentos/Orcamentos.tsx#L403)) estourava **sozinho e por outro motivo**: `justify-between` sem `flex-wrap` **e** o grupo interno de botões somando ~327px ("PDF Agrupado" + "Novo Orçamento") contra 280px — ou seja, `flex-wrap` só no wrapper externo não resolveria, o grupo interno precisou de `flex-wrap` também. **Fix:** markup único com variantes `md:` (padrão `ContasReceber.tsx:247-344`, sem duplicar bloco `hidden md:flex`) — raiz `flex flex-col md:flex-row md:items-center gap-2 md:gap-4`; novo wrapper checkbox+conteúdo `flex items-start md:items-center gap-3 md:gap-4 flex-1 min-w-0`; bloco de conteúdo ganhou `min-w-0` (é ele que autoriza o encolhimento), linha nome+status ganhou `flex-wrap` + `shrink-0` no badge, linha de meta virou `gap-2 md:gap-4 flex-wrap`; nova barra valor+ações `flex items-center justify-between md:justify-end gap-2 md:gap-4 flex-wrap md:flex-nowrap shrink-0 pt-2 md:pt-0 border-t md:border-t-0` com valor `text-left md:text-right` e ações `shrink-0 ml-auto md:ml-0`. Header e lista `p-6` → `p-4 md:p-6` (devolve 32px de largura no mobile e alinha ao padrão de Financeiro/ContasReceber/Recibos/LeadsList — Orçamentos era o fora-do-padrão); header ganhou `flex-wrap gap-3` no wrapper **e** `flex-wrap` no grupo de botões; cabeçalho de cliente do modo seleção ([:464](src/modules/orcamentos/Orcamentos.tsx#L464)) ganhou `flex-wrap gap-2` + `min-w-0`. Toda a lógica de PDF/PIX/editar/excluir/assinatura e os breakpoints `sm:` internos ficaram intactos. **Desvio consciente do plano:** o `tabular-nums` que o ContasReceber usa no total **não** foi adotado — é `font-variant-numeric`, mudaria a largura dos glifos **também no desktop**, contra o requisito de desktop idêntico; ficou só `md:whitespace-nowrap` (risco zero: impede uma quebra que no desktop nunca acontece). **Bônus:** `leads.find(l => l.id === orc.leadId)` rodava 2× por card ([:338](src/modules/orcamentos/Orcamentos.tsx#L338) e [:346](src/modules/orcamentos/Orcamentos.tsx#L346)) — hoistado para `const lead` no topo de `renderOrcCard`. **Verificação:** `tsc --noEmit` 0 erros; `vite build` limpo. Medido em Puppeteer headless (fixture isolado replicando `MainLayout` + card/header de Orçamentos, servido com o CSS **realmente compilado**; lado "before" com um build separado do HEAD `17f4b8e1`, mesma técnica dos v2.36.10/11): **360px** — linha do card `scrollWidth/clientWidth` 374/246 → **262/262**, botão PDF `right` 370 (**fora** da viewport de 360) → 250, botão PIX `right` 431 (**71px além da borda**) → 311, ambos dentro; **390px** — PIX `right` 431 (**41px fora**) → 341, dentro; botões seguem 60×44 e 57×44 (regra 9 preservada); altura do card 133 → 146px. **Desktop 1440px: screenshots before × after com o mesmo SHA-256 (`72f552d769254a68`)** e paridade exata de todos os rects (valor `x:1045 w:108`, ações `x:1169 w:206`, linha do card 1054/1054) — desktop provado intocado. **Achado que corrige o critério de validação proposto:** `document.documentElement.scrollWidth === clientWidth` dava **true nos dois lados** e teria mascarado o bug — o `<main>` é `overflow-y-auto`, e pela spec CSS quando um eixo não é `visible` o outro computa para `auto`, então o `overflow-x` do `<main>` é `auto` e o arrasto lateral vive **dentro do `<main>`**, não no documento. A prova correta é `main.scrollWidth − clientWidth`: **71px @360, 41px @390 → 0px nos dois** depois do fix. **Faixa 640–767px** (entre `sm:` e `md:`): o card passa a ficar empilhado enquanto os botões já são só ícone e o grupo `hidden sm:flex` já apareceu — medido em 744px, sem estouro (linha 646/646), altura do card 92 → 146px; é a única faixa onde o layout muda sem ser mobile nem desktop. **Pendência:** confirmação visual sua no celular (360/390) — PDF e PIX visíveis sem arrastar.
- **Fix v2.36.12 (varredura do padrão `h-full` + `flex-1 overflow-y-auto` em TODAS as telas):** como o mesmo colapso de lista no mobile estourou em 2 telas independentes (Financeiro v2.36.10, ContasReceber v2.36.11), esta rodada varreu o projeto inteiro atrás da mesma bomba latente — a dívida que os dois fixes anteriores deixaram registrada. **Método:** em vez de inspeção visual (que já mascarou esse bug uma vez), o mapa foi montado pela aritmética de altura da cadeia do `MainLayout`: `h-full` de um módulo vale `100vh − ~112px` (header global em 2 linhas: barra principal + WorkspaceSwitcher) `− 72px` (`BottomNav`, que é **item flex em fluxo**, `flexShrink:0`, **não** `fixed`) `− 88px` (`pt-4` + `pb-[calc(72px+safe-area)]` do `<main>`) ≈ **395px num iPhone SE (667px)** e ≈572px num 390×844. É desse orçamento que o header de cada módulo desconta antes de a lista receber o resto — e como o header irmão tem `overflow:visible` (`min-height:auto` = altura do conteúdo, não encolhe) e a lista é item flex com `overflow≠visible` (mínimo automático 0, spec flexbox §4.5), tudo que o header consumir sai da lista. **Resultado da varredura — 11 ocorrências, 4 grupos:** (a) **6 telas corrigidas com o mesmo fix já validado** — `Notas.tsx` (79/125), `LeadsList.tsx` (60/184), `Orcamentos.tsx` (400/441), `Recibos.tsx` (149/179), `Fornecedores.tsx` (402/423, raiz com a ordem invertida `flex flex-col h-full`), `IAAssistente.tsx` (195/215); em todas: raiz `h-full flex flex-col` → `flex flex-col md:h-full` e lista `flex-1 overflow-y-auto p-X` → `p-X md:flex-1 md:overflow-y-auto`, **mantendo o padding existente de cada arquivo** (`p-6` continua `p-6`, `p-4 md:p-6` continua `p-4 md:p-6`) — só `flex-1` e `overflow-y-auto` foram para trás do `md:`. (b) **2 telas com o padrão mas deixadas de fora por particularidade real:** `Kanban.tsx` (o board é `flex-1 overflow-x-auto` com `flex h-full min-w-max` e colunas `min-h-[600px] pb-32` ≈728px dentro de uma janela de ~300px no mobile — soltar o `h-full` faria a página rolar vertical **e** horizontal ao mesmo tempo e mexeria no containing block do `@dnd-kit`; precisa de decisão de UX própria, tipo colunas empilhadas/accordion) e `WhatsApp.tsx` (layout de chat: a altura travada ali é **requisito**, é o que mantém o composer colado no rodapé e o auto-scroll das mensagens funcionando — o "scroller único" quebraria a tela; no mobile só um painel aparece por vez via `hidden`/`flex`, header curto ~110px, então está degradado e não inacessível). (c) **Falsos positivos confirmados:** `LeadDetail.tsx` tem o padrão idêntico mas **não sofre** — só é renderizado dentro do `SlidePanel` (`LeadsList.tsx:244`, `Kanban.tsx:136`), que é `fixed ... h-full` (`Modal.tsx:97`) → containing block = viewport, altura definida, **fora do `<main>`**, `body` com `overflow:hidden`, header ~122px sobre ~600px; `Modal`/`AILeadModal`/`SignatureModal` são `max-h-[90vh]` (altura **máxima**, não travada — o scroller interno ali é o correto); `HelpPanel`/`Sidebar` são overlays `fixed`. (d) **Referência de layout correto:** Dashboard, Settings, Operacional, Central, Estoque, ComparadorPrecos e AdminEmpresas **não usam `h-full`** — já fluem com o `<main>` como scroller único, que é exatamente o estado para onde as 6 telas foram levadas. **Gravidade medida por estimativa de header (375px):** `Notas` é o caso terminal e **pior que o Financeiro** — é o único módulo com **3 filhos em fluxo** (header ~240px + banner de aviso `mx-6 mt-6` ~145px = ~385px de 395px), sobrando **~10px de lista**; as outras 5 não colapsam a zero (lista 215–310px) mas sofrem a mesma classe de defeito em grau menor: com a raiz travada em `h-full` o conteúdo cabe exato no `<main>`, **o `<main>` nunca rola**, e 100% do scroll acontece numa janelinha cuja borda inferior fica atrás da barra dinâmica do navegador (porque `h-screen`=100vh > área visível — o mesmo motivo do `pb` do v2.36.8). O banner do `Notas` **não** foi tocado: sem `h-full` no mobile ele simplesmente passa a rolar junto com a página. **Verificação:** `tsc --noEmit` 0 erros; `vite build` limpo em 7.85s (só o aviso pré-existente de bundle >500KB); grep confirmando que as 12 linhas alteradas ficaram com a forma exata de `Financeiro.tsx`/`ContasReceber.tsx` e que `Kanban`/`WhatsApp`/`LeadDetail` seguem intactos. **Risco ao desktop: nulo** — toda declaração removida do mobile volta sob `md:`, nenhum dos 6 arquivos usa `md:h-*`/`md:flex-*`/`md:overflow-*` nessas divs (sem disputa de ordem no Tailwind), e a transformação é literalmente a mesma que já está em produção há duas versões. **Pendências:** (1) confirmação visual em celular real pelo usuário nas 6 telas — as medições desta rodada são estimativas de CSS, **não** medições em Puppeteer como nos v2.36.10/11; (2) checar `document.documentElement.scrollWidth == clientWidth` em cada tela: sem `overflow-y-auto` o `overflow-x` da lista volta a `visible`, e conteúdo largo deixa de ser clipado e passa a empurrar a página (foi exatamente o efeito colateral que o ContasReceber teve). Dois pontos a observar se isso acontecer, ambos **pré-existentes e fora do scroller** (o fix não os piora): `Orcamentos.tsx:403` e `IAAssistente.tsx:198`, headers `justify-between` **sem `flex-wrap`** — tratamento seria o mesmo do ContasReceber (`flex-wrap md:flex-nowrap` + `ml-auto md:ml-0`), em commit separado. **Achado que corrige uma suposição:** o `pb-[calc(72px+safe-area)]` do `<main>` **parece** redundante com o `BottomNav` estando em fluxo, mas é intencional (v2.36.8) e **não deve ser removido** — é o colchão contra a barra dinâmica do navegador.
- **Fix v2.36.11 (2 bugs: filtro de mês do Financeiro + card mobile do ContasReceber cortado):** dois reportes de produção em celular real, pós-v2.36.10. Só 2 arquivos tocados (`Financeiro.tsx`, `ContasReceber.tsx`).
  - **Bug 1 — "trocar agosto→julho não carrega as transações de julho". A premissa do reporte estava errada, e a investigação achou um bug real diferente.** Consulta ao banco de produção (`maadmoayrogrhntlyqvn`, o mesmo do `.env`): a tabela `transactions` tem **3 linhas no total, todas de maio/2026** (última criada em 20/05/2026) — **não existe transação de julho nem de agosto**. Os "dados de teste de julho" estão em `contas_receber` (2 linhas em julho), outra tabela/outro módulo — daí o ContasReceber "funcionar" e o Financeiro parecer quebrado (agosto vazio, julho vazio, navegar e voltar não muda nada porque não há o que carregar). **Mas o defeito real está na mesma cadeia, no formato:** `transactions.data` é `timestamptz` no Postgres (`2026-05-05 00:00:00+00`), enquanto `contas_receber.data_vencimento` é `date`; e `formatters.ts:67` faz `data: raw.data` **cru** — é o **único** campo de data do arquivo que não passa por `toDateInput()` (`formatContaReceber` normaliza, `:97`). O frontend recebia `"2026-05-05T00:00:00+00:00"` e `parseISO` convertia para o fuso local (UTC-3) → **2026-05-04 21:00**. Consequências medidas: (a) toda transação exibia **um dia antes** do lançado ("05/05" como "04 de mai"); (b) lançamento no **dia 1 de qualquer mês caía no mês anterior** no `isWithinInterval` — o seletor de mês genuinamente mostrava no mês errado; (c) o CSV exportava o timestamp cru. Evidência de que sempre foi um problema: `TransactionForm` já carregava um `normalizeDate` privado que tirava o `T` — contorno local que só existia porque `data` nunca foi normalizado. **Fix (só `Financeiro.tsx`):** `normalizeDate` hoistado para o escopo do módulo (com o comentário do porquê) e o `normalizeDate` duplicado do form apagado; filtro de mês trocado de `startOfMonth`/`endOfMonth`/`isWithinInterval` por comparação de string — `normalizeDate(t.data).startsWith(selectedMonth)` — que casa direto com o `"AAAA-MM"` do `input[type=month]`, sem fuso no meio; ordenação passou a `localeCompare` sobre `"AAAA-MM-DD"` (mesma ordem de antes); exibição da data do card e coluna Data do CSV normalizadas. `t.dataVencimento`/`t.dataPagamento` **não** mudaram (já vinham normalizados por `toDateInput`). Ganho colateral: `monthStart`/`monthEnd` eram recriados a cada render, então o `useMemo` nunca memoizava; agora a dependência é uma string. `stats`/`statusCounts`/"Resultado do Mês" já derivavam de `monthTransactions` — acompanham o mês e agora **corretamente**. Imports `startOfMonth`/`endOfMonth`/`isWithinInterval` removidos (obrigatório: `noUnusedLocals: true`). **Decisão:** a normalização ficou local no Financeiro em vez de `formatters.ts` (regra "só os 2 arquivos") — verificado que `Financeiro.tsx` é o **único** consumidor de `transaction.data` sensível a data (`dashboardSelectors.ts:14` soma receita sem filtrar por data), então o raio de impacto é idêntico ao de um fix na origem.
  - **Bug 2 — "card mobile AINDA corta valor e botões"; o fix anterior atacou a camada errada.** A causa está **fora do bloco do item**: `ContasReceber.tsx` continuava `h-full flex flex-col` com a lista em `flex-1 overflow-y-auto` — **exatamente o colapso diagnosticado no v2.36.10, que foi corrigido só no Financeiro**. Header (`p-4` + título + 4 StatCards 2×2) + faixa de filtros consomem quase toda a altura do `<main>`, e a lista, sendo item flex com `overflow≠visible`, colapsa (spec flexbox §4.5, mínimo automático = 0). Medido em Puppeteer com o CSS realmente compilado: `lista.clientHeight` = **32px / 93px / 164px** (360×640, 390×844, 412×915) contra `scrollHeight` ~850-900px, e — rolando **só o `<main>`**, que é o gesto que o dedo faz — a **altura visível do último card era 0px nas 3 larguras**: valor e botões ficavam a ~1.300px de distância, dentro de um scroller interno que na prática não dá para alcançar. Nenhuma mudança interna ao card resolveria. **Fix (a) estrutural, mesmo do v2.36.10:** raiz `h-full flex flex-col` → `flex flex-col md:h-full`; lista `flex-1 overflow-y-auto p-4 md:p-6` → `p-4 md:p-6 md:flex-1 md:overflow-y-auto` — no mobile o `<main>` volta a ser o único scroller (reaproveita o `pb-[calc(72px + safe-area)]` do v2.36.8). **Fix (b) markup único:** os dois blocos duplicados (`hidden md:flex` + `flex md:hidden`, ~185 linhas com o conteúdo repetido literalmente) viraram **um** bloco de ~95 linhas com variantes `md:`, no padrão do Financeiro: wrapper `flex-col md:flex-row md:items-center`, bloco ícone+conteúdo `flex-1 min-w-0`, barra valor+ações `justify-between md:justify-end` com `pt-2 border-t` no mobile e `md:pt-0 md:border-t-0` no desktop. **Desvio consciente do padrão do Financeiro, medido:** a barra do ContasReceber carrega badge **e** valor **e** 3 ações (≈335px necessários contra ≈296px úteis dentro do card em 360px), então ganhou `flex-wrap md:flex-nowrap` + `ml-auto md:ml-0` nas ações — cabendo (412px, ou 2 botões) fica em linha única com `justify-between`; não cabendo (360/390px) as ações caem para uma 2ª linha alinhada à direita. Sem isso o estouro voltaria, e agora *sem* `overflow-y-auto` na lista ele empurraria a página inteira na horizontal em vez de ser clipado. **Touch targets:** mantido `<Button variant="ghost" size="sm">` (já dá `min-h-[44px]`) + `min-w-[44px] md:min-w-auto` — mobile 44×44 (regra 9), desktop em `min-width:auto`, ou seja idêntico ao de hoje; **não** foi adotado o 44/36 do Financeiro justamente porque encolheria os botões do desktop de 44px para 36px. Fechada também a dívida `px-6` fixo da faixa de filtros (`px-4 md:px-6`), registrada no v2.36.10.
  - **Verificação:** `tsc --noEmit` 0 erros; `vite build` limpo (só o aviso pré-existente de bundle >500KB); as 19 classes novas confirmadas no CSS compilado (inclusive `md:min-w-auto`, que existe no Tailwind v4 — não precisou do fallback `md:min-w-[auto]`). **Bug 1 provado sem login Supabase:** script node com `TZ=America/Sao_Paulo` rodando o filtro antigo e o novo sobre o shape real do PostgREST — antes: `2026-05-05` exibia "04 de mai", `2026-05-20` exibia "19 de mai" e um lançamento de `2026-06-01` aparecia em **maio** e sumia de junho; depois: datas corretas e o dia 1 em junho; ordenação idêntica; agosto vazio nos dois (correto, não há dados). **Bug 2 medido em Puppeteer headless** (fixture isolado replicando `MainLayout` + `ContasReceber` com dados reais de junho/2026, servido com o CSS de cada lado — o "before" foi construído com um build separado do HEAD via `git stash`, não com reinjeção de regra): mobile 360/390/412 — lista deixou de ser scroller interno (`overflow-y` `auto`→`visible`, `clientHeight` 32/93/164 → 897/836/706), último card **alcançável** rolando só o `main`, rodapé (badge+valor+3 ações) **visível e completo** (altura visível do card 0px → 225/198/164px), botões 38×44 → **44×44**, zero overflow horizontal (`docScrollW == docClientW`) nas 3 larguras. **Desktop 1280×800: as duas capturas são idênticas byte a byte** (mesmo SHA-256), com paridade em todas as métricas geométricas (rects de badge/valor/ações, tamanho dos botões 38×44/37×44/38×44, `list.scrollHeight` 454 vs `clientHeight` 403 — o scroller interno do desktop segue intacto). **Pendência:** confirmação visual no celular real pelo usuário. **Dívida seguinte (não tocada):** o par `h-full` + `flex-1 overflow-y-auto` continua ativo em Kanban, Notas, LeadsList, LeadDetail, Recibos, Orcamentos, IAAssistente e WhatsApp — mesmo risco latente; já são 2 telas estouradas (Financeiro, ContasReceber), vale generalizar. Observado e **não** corrigido (fora de escopo): `formatters.ts:67` segue entregando `transaction.data` cru; `dashboardSelectors.ts:14` soma **todas** as receitas sem filtrar por mês apesar do nome `receitasMes`; `getStatusDisplay` (`Financeiro.tsx:44`) compara com `new Date().toISOString()` (data em UTC), adiantando "hoje" em um dia depois das ~21h de Brasília; header do ContasReceber (`:163`) sem `flex-wrap` (o do Financeiro tem).
- **Fix v2.36.10 (mobile: página Financeiro não rolava — lista de transações inacessível):** reportado em produção em celular real — header/stats/filtros apareciam, a barra de scroll indicava conteúdo abaixo, mas o gesto de rolagem nunca alcançava a lista de transações. Desktop OK. **Causa raiz (dois scrollers aninhados + altura fixa):** `Financeiro.tsx` era `h-full flex flex-col` (trava a altura em `main.height − p-4 − pb-[72px+safe-area]`, ~463px num 360×640) com a lista interna em `flex-1 overflow-y-auto` — **um segundo scroller aninhado dentro do `main`**, que já é `overflow-y-auto`. O header mobile do Financeiro (título + 3 controles que embrulham + 4 StatCards em 2×2 com `text-2xl` + chips de status + select de categoria) mede **542px**, ou seja mais que os 463px disponíveis. O header tem `overflow: visible`, então `min-height: auto` = altura do conteúdo e **ele não encolhe**; a lista é `flex: 1 1 0%` **com `overflow-y: auto`**, e pela spec do flexbox (§4.5) o mínimo automático de um item com overflow ≠ visible é **0** → **a lista colapsava para 0px de conteúdo** (medido: `listH = 32px`, que é só o `p-4`, com `scrollHeight = 2909px` de conteúdo dentro). A única barra de scroll visível era a do `main`, vinda do transbordo do header (`scrollHeight 590` vs `height 463`) — daí "tem conteúdo abaixo mas a rolagem não alcança". Efeito colateral medido: a descrição da transação tinha **0px de largura** em 360px (o bloco valor+ações era `shrink-0` com ~215px). **Por que o ContasReceber rolava:** estrutura idêntica, mas header mais baixo (2 controles, chips numa faixa separada, `valueClassName="text-lg md:text-2xl"`) → a lista ficava com altura > 0 e o scroll interno funcionava. **Fix (só `src/modules/financeiro/Financeiro.tsx`, tudo escopado em `md:`):** (1) raiz `h-full flex flex-col` → `flex flex-col md:h-full`; (2) lista `flex-1 overflow-y-auto p-4 md:p-6` → `p-4 md:p-6 md:flex-1 md:overflow-y-auto` — no mobile o `main` volta a ser o **único** scroller, reaproveitando o `pb-[calc(72px + env(safe-area-inset-bottom))]` que o `MainLayout` já ganhou no v2.36.8 (nenhuma mudança no `MainLayout`). Auditoria dos mesmos bugs do ContasReceber aplicada no mesmo arquivo: (3) `input[type=month]` ganhou `shrink-0 min-w-[150px]` (mesmo fix do dropdown cortado, `ContasReceber.tsx:168`); (4) bloco "Resultado do Mês" empilha no mobile (`flex-col md:flex-row md:items-center gap-2`, `px-4 md:px-5`) e a linha Receitas/Despesas virou `flex-wrap` — mesma classe de bug do rodapé corrigido em `c6c074d6`; (5) linha do card empilha no mobile (padrão Estoque/ContasReceber): novo wrapper `flex items-start gap-3 flex-1 min-w-0` para ícone+conteúdo e o bloco valor+ações virou `justify-between md:justify-end pt-2 md:pt-0 border-t md:border-t-0` — **markup único com variantes `md:`** em vez da duplicação `hidden md:flex`/`flex md:hidden` que o ContasReceber usou (~95 linhas repetidas), evitando divergência entre as duas cópias; (6) touch targets dos 3 botões de ação `min-w/h-[36px]` → `min-w-[44px] min-h-[44px] md:min-w-[36px] md:min-h-[36px]` (regra 9). Item "`p-6` fixo → `p-4 md:p-6`" da auditoria fechou **sem mudança**: o Financeiro já estava 100% em `p-4 md:p-6`. **Verificação:** `tsc --noEmit` 0 erros; `vite build` limpo (só o aviso pré-existente de bundle >500KB). Validação de layout automatizada **sem depender de login Supabase** (mesma técnica do v2.36.5): fixture isolado replicando `MainLayout` + `Financeiro` servido com o CSS **realmente compilado** pelo build, medido em Puppeteer headless nas variantes before/after — 360×640, 390×844, 412×915 e 1280×800. Resultados: mobile antes `listH` 32/37/108px com último card **inalcançável** → depois 2381px, `main.scrollHeight` 3012px, último card **alcançável** nas 3 larguras, zero overflow horizontal (`docScrollW == docClientW`), descrição de 0px → 214-247px, botões 44×44. **Desktop 1280×800: todas as 18 métricas idênticas** entre before e after (inclusive `listScrolls: auto` + `listScrollH 1110` vs `listH 435` — o scroll interno do desktop segue intacto — e botões 36×37). Detalhe do harness: o CSS deste build não emite mais `.min-w-[36px]` sem prefixo (nada usa), então a regra foi reinjetada na posição original da cascata para o lado "before" medir o que a produção tem hoje. **Dívida documentada (não tocada):** o mesmo par `h-full` + `flex-1 overflow-y-auto` existe em Kanban, Notas, LeadsList, LeadDetail, Recibos, Orcamentos, IAAssistente e WhatsApp — todos com o mesmo risco latente de colapso da lista se o header crescer no mobile; só o Financeiro estourou. Também ficaram de fora (escopo "nenhuma outra tela"): `ContasReceber.tsx:535` (form do modal ainda `p-6` fixo) e `ContasReceber.tsx:211` (faixa de filtros ainda `px-6` fixo). **Pendência:** confirmação visual em celular real pelo usuário (as medições cobrem geometria, não percepção).
- **UX v2.36.9 (Contas a Receber: stats voltam a 2 colunas no mobile, valor menor):** o fix anterior (v2.36.8-adjacente, não versionado à parte) tinha posto o grid de stats em `grid-cols-1` no mobile pra evitar corte do valor (`text-2xl`), mas isso gerou scroll vertical excessivo (4 cards empilhados). Usuário pediu 2 colunas de volta, com fonte do valor menor só ali. **Achado:** `Financeiro.tsx` já roda esse exato padrão em produção (`grid-cols-2 md:grid-cols-4 gap-3`, header `p-4 md:p-6`) sem prop de fonte nova — `ContasReceber.tsx` divergia (header `p-6` fixo, grid `grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-4`). Alinhado ao padrão do Financeiro (mais espaço horizontal no mobile) e, como valores de Contas a Receber tendem a ter mais dígitos, `StatCard` (`src/components/ui/Card.tsx`) ganhou prop opcional nova `valueClassName?: string` — quando omitida (todo uso em `Dashboard.tsx`/`Financeiro.tsx`), resultado é idêntico a antes (`font-bold text-[var(--text-primary)] text-2xl`, mesmas classes só reordenadas); só `ContasReceber.tsx` passa `valueClassName="text-lg md:text-2xl"` nos 4 `StatCard`. Único componente compartilhado tocado com mudança 100% opt-in — Dashboard/Financeiro inalterados. `tsc --noEmit`: 0 erros. `vite build`: limpo (só aviso pré-existente de bundle >500KB). **Pendência:** validação visual em dispositivo/viewport mobile real não foi automatizada nesta sessão — app exige login Supabase real (`App.tsx:275`) sem credenciais de teste disponíveis (mesma limitação já registrada na pendência do fix de Estoque); usuário optou por validar manualmente (Contas a Receber 360px: 2×2, valores completos sem corte; Dashboard/Financeiro: inalterados).
- **Fix v2.36.8 (mobile: fim de listas longas escondido atrás do BottomNav):** bug global de layout, não de tela específica — reportado em Contas a Receber, mas afeta qualquer lista rolável no mobile (Leads, Orçamentos, Recibos etc.), já que todas passam pelo mesmo `<main>` de `MainLayout.tsx`. `BottomNav.tsx` não é `position:fixed` — é irmão flex normal de `<main>` na coluna flex (`main` = `flex-1 overflow-y-auto`, `BottomNav` = `height:72` + `env(safe-area-inset-bottom)`, `flexShrink:0`), então em teoria o flexbox já reserva o espaço certo; na prática `h-screen` (100vh) não acompanha de forma confiável a barra de endereço dinâmica dos navegadores mobile, e o fim do conteúdo ficava colado/atrás do nav. Fix único e mínimo: `MainLayout.tsx:133`, `<main>` ganhou `pb-[calc(72px_+_env(safe-area-inset-bottom,0px))]` (mobile) — `md:p-6` já existente sobrescreve pra 24px no desktop automaticamente (BottomNav é `md:hidden`, não precisa de reserva lá), sem precisar de `md:pb-0` extra. Nenhuma tela tinha workaround próprio (confirmado via grep por `safe-area-inset-bottom`/`pb-2x`/`pb-[` em `src/` antes do fix — só aparecia dentro do próprio `BottomNav.tsx`), então nenhuma tela precisou de mudança. Único arquivo tocado. `tsc --noEmit`: 0 erros. `vite build`: limpo (só aviso pré-existente de bundle >500KB). **Pendência:** validação visual em dispositivo mobile real não foi automatizada nesta sessão — usuário vai validar rolando até o fim de Contas a Receber/Leads.
- **UX v2.36.7 (Estoque: tabela ilegível no mobile vira card):** `src/modules/suprimentos/Estoque.tsx` renderizava a lista de materiais como um CSS Grid 100% inline-style (`display:'grid'`, sem nenhuma classe Tailwind) sem nenhum tratamento responsivo — em <768px o cabeçalho colava e as células quebravam na vertical. **Achado da investigação:** não existe no repo um padrão "tabela desktop + card mobile" pra copiar literalmente — Recibos/Orçamentos/Fornecedores nunca tiveram `<table>`, usam uma única lista de cards sempre visível que só reflui internamente (`flex-col sm:flex-row`, breakpoint 640px) e não têm campos rotulados (nome/categoria aparecem como ícone+valor, sem label). Como Estoque precisava especificamente de campos rotulados (decisão confirmada com o usuário: Nome como título do card + Status como badge, sem rótulo textual "Nome:"/"Status:"; Categoria/Unidade/Estoque/Mínimo em grid 2 colunas com rótulo), a reutilização aconteceu em 3 níveis em vez de copiar um componente: (1) mecanismo de breakpoint `hidden`/`md:hidden`/`md:block` do Tailwind, já usado em `Sidebar.tsx`/`BottomNav.tsx`/`MainLayout.tsx`/`HeaderGlobal.tsx`; (2) `getStatus()`/`StatusBadge`/tons de fundo por status já existentes no próprio arquivo, reaproveitados 1:1; (3) touch target ≥44px via novo const `mobileIconBtn` (spread de `iconBtn` + `minWidth/minHeight:44`), sem alterar o `iconBtn` usado no desktop/modais. Extraído também `fieldLabelStyle` (estilo de rótulo do cabeçalho da tabela) e `EMPTY_MATERIAIS_MSG`, reaproveitados nos dois blocos — refactor seguro, zero mudança visual no desktop. Único arquivo tocado. `tsc --noEmit`: 0 erros. `vite build`: limpo (só aviso pré-existente de bundle >500KB). **Pendência:** validação visual no navegador (mobile 360-412px + desktop) não foi automatizada nesta sessão — requer login Supabase real, sem credenciais de teste disponíveis; usuário optou por testar manualmente.
- **Fix v2.36.6 (mobile: Sidebar sem backdrop deixava BottomNav e fundo clicáveis por baixo):** ao abrir o menu hambúrguer no mobile, o `<aside>` da Sidebar (`src/layout/Sidebar.tsx`) só cobre `w-64` (256px) de largura — o resto da viewport, incluindo o `BottomNav` inteiro (que não é `position:fixed`, é só um item flex sem z-index algum), continuava visível e clicável por baixo, sem nenhum backdrop e sem scroll-lock do body. Único arquivo tocado: `Sidebar.tsx`. **Causa/fix:** adicionado backdrop (`fixed inset-0 z-40 bg-black/40 backdrop-blur-sm`, `md:hidden`, `onClick` fecha) como irmão do `<aside>` dentro de um Fragment; `<aside>` sobe de `z-40`→`z-50` (camada de conteúdo/painel, mesma convenção já usada em `Modal`/`SlidePanel` e no dropdown de usuário do `MainLayout` — backdrop=40/painel=50). Scroll-lock via `useEffect` em `mobileOpen` (`document.body.style.overflow`, cleanup no unmount) — mesmo padrão duplicado em `Modal.tsx`/`SlidePanel` (`src/components/ui/Modal.tsx`), sem extrair hook novo. Cliques nos itens de menu (loop de seções + item Admin) ganharam `setMobileOpen(false)` — antes só o botão X fechava. **`MainLayout.tsx` e `BottomNav.tsx` não precisaram de nenhuma mudança**: um backdrop `fixed` com z-index positivo sempre pinta acima de um descendente não-posicionado (ordem de pintura CSS2.1, categoria "positioned z-index positivo" > "in-flow não-posicionado"), independente de nesting no DOM — confirmado que nenhum wrapper do `MainLayout` cria stacking context/containing block (`overflow-hidden` sozinho não cria), então o backdrop cobre e intercepta cliques no `BottomNav` só por CSS. Desktop intocado (`md:hidden` no backdrop; `<aside>` mantém `md:relative`/`md:translate-x-0` como antes) — inclusive no caso de resize mobile→desktop sem fechar a sidebar (`mobileOpen` fica `true` no state, mas `md:hidden` já esconde o backdrop via media query, reativa a resize). `tsc --noEmit`: 0 erros. `vite build`: limpo (só o aviso pré-existente de bundle >500KB).
- **Fix v2.36.5 (header mobile em 2 linhas — título truncado + subtítulo estourado):** o fix anterior do mesmo dia (`14c9cdd4`, remover `hidden md:block` do WorkspaceSwitcher pra expô-lo no mobile) sobrecarregou a linha única do header em telas ≤412px: título truncava ("Da...") e o subtítulo (sem `max-width` abaixo de `md:`) quebrava palavra-por-palavra, inflando a altura. Fix estrutural: header vira 2 linhas no mobile. `src/layout/HeaderGlobal.tsx` — data e subtítulo ganham `hidden md:block` (somem no mobile, voltam no desktop); WorkspaceSwitcher e botão Ajuda ganham `hidden md:flex` (saem da linha 1 mobile — Ajuda já tinha equivalente funcional na Sidebar via "Central de Ajuda", mesmo `onOpenHelp`, nenhuma mudança lá). `src/layout/MainLayout.tsx` — a linha do header (hambúrguer+HeaderGlobal+avatar) ganhou um wrapper `flex-col` e uma 2ª linha `md:hidden` abaixo, reaproveitando o **mesmo** `<WorkspaceSwitcher/>` (import novo) — fica montado 2× no DOM (uma cópia oculta via CSS em cada breakpoint, trade-off aceito: fetch Supabase duplicado, é leitura pequena; alternativa de hook de media query fugiria do padrão 100%-CSS já usado no projeto). `src/layout/WorkspaceSwitcher.tsx` — nome da empresa (`span`) deixou de ser `hidden md:inline`, agora sempre visível com `truncate`/`shrink-0` nos ícones (necessário pra aparecer na linha 2 mobile; lógica de troca/fetch intocada). **Achado não previsto durante a verificação:** `index.css` tem uma regra `h1{font-size:1.5rem}` **fora de qualquer `@layer`** — no CSS Cascade Layers isso sempre vence utilitários Tailwind (que ficam dentro de `@layer utilities`) independente de especificidade, então `text-base`/`md:text-lg`/`text-sm` no título do header sempre foram **no-op** (título sempre renderizou 24px, inclusive no desktop antes deste fix). Confirmado via `getComputedStyle` num fixture isolado servindo o CSS realmente compilado pelo build (`vite preview` + Playwright/Edge headless, sem exigir login Supabase). Corrigido restringindo o footprint ao necessário: `text-base` → `text-sm!` (modifier `!important` do Tailwind v4) só no mobile, mantendo `md:text-lg` como estava (continua no-op no desktop — 24px idêntico a antes, zero regressão). Testado nas 5 larguras pedidas (360/375/390/412/desktop) × 5 rotas (Dashboard/IA/Configurações/Fornecedores/Comparador — única sem subtítulo): título sempre completo, sem quebra de palavra, subtítulo/data ocultos no mobile e presentes no desktop, seletor de workspace com nome completo na linha 2. `tsc --noEmit`: 0 erros. `vite build`: limpo (só o aviso pré-existente de bundle >500KB). Nenhuma mudança em `Sidebar.tsx`, `BottomNav.tsx`, `useStore.ts`, backend ou lógica de troca de workspace.
- **Fix v2.36.4 (IA Assistente e Criar Lead com IA nunca usavam a resposta real da OpenAI):** dois bugs distintos investigados a fundo (a chamada de rede em "Analisar Lead" sempre retornava 200 com conteúdo real, mas a UI nunca mudava — sintoma reportado por cliente). **Bug 1 (`src/modules/ia/openaiStrategicService.ts` + `strategicDiagnosisService.ts`):** `refineLeadStrategyWithAI` pedia à IA um JSON com chaves `ajusteMensagem`/`ajusteEstrategia`/`observacaoExtra`/`riscoReavaliado`, mas `generateFullStrategicDiagnosisWithAI` fazia `{...baseDiagnosis, ...refined}` — nenhuma dessas 4 chaves existe em `StrategicDiagnosis` (que usa `resumoExecutivo`/`estrategiaDeAbordagem`/`mensagemSugeridaWhatsApp`/`riscoDePerda`/etc), então o spread era um no-op funcional: a UI sempre mostrava o diagnóstico 100% heurístico local, mesmo com a chamada de rede tendo sucesso. Fix: `OpenAIStrategicResponse` virou `Partial<Pick<StrategicDiagnosis, ...>>` (mesmas chaves da UI, via `import type` — sem ciclo em runtime), prompt reescrito para pedir essas chaves diretamente, `response_format:{type:'json_object'}` adicionado, `!response.ok` agora checado, e todo caminho de falha (HTTP erro, conteúdo ausente, `JSON.parse` malformado) loga `console.error` explícito em vez de retornar `null` mudo. Novo campo obrigatório `aiUsed: boolean` em `StrategicDiagnosis` (true só quando a chamada teve sucesso real — inclusive quando a IA responde `{}` de propósito dizendo "está adequado"; false só em falha de verdade) alimenta um banner discreto no preview do `IAAssistente.tsx` ("Análise simplificada — IA indisponível", mesmo padrão visual `--warning-subtle`/`AlertTriangle` já usado em `AILeadModal.tsx`). **Bug 2 (`src/services/ai/ai.service.ts` + `AILeadModal.tsx`):** "Criar Lead com IA" nunca chamava a OpenAI — `analyzeConversation` é inteiramente regex/keyword-matching local (extração de telefone/nome/serviço por palavras-chave fixas de serralheria); a "IA" era só branding. Fix: novo método `analyzeConversationWithAI` chama `/api/chat` de verdade (mesmo padrão de `apiFetch`, prompt pedindo JSON com nome/telefone/email/endereço completo/serviço incluindo medidas e materiais/origem/prazo do cliente/temperatura/status/valor/visita sugerida+data+período/resumo rico), com a heurística local rodando sempre primeiro como piso garantido e `mergeAIWithHeuristic` sobrescrevendo campo a campo só onde a IA confirmou um valor válido (contra listas de enum) — fallback heurístico completo só ativa em falha real de rede/HTTP/parse (mesmo padrão de log + `aiUsed` do Bug 1), nunca quando a IA simplesmente não identificou um campo. Duas heurísticas locais que já existiam mas nunca eram usadas foram ligadas: `detectVisitPeriod` (código morto, nunca chamado) e nova `detectPrazoCliente` (mesma técnica de `detectOrigin`/`detectTemperature`) — `AIAnalysisResult` ganhou `prazoCliente`/`visitaOrcamentoPeriodo`/`aiUsed`, e `convertToLead` passou a preencher `prazoCliente` de verdade (era hardcoded `'nao_definido'`) e os campos `visitaOrcamentoData`/`visitaOrcamentoPeriodo` do Lead (existiam no tipo e no mapeamento `formatters.ts`, nunca eram escritos por este fluxo). **Bug 3, achado durante a investigação (`src/services/database/lead.service.ts createLead`):** o INSERT usava uma whitelist de só 9 campos (nome/telefone/email/endereco/servico/status/temperatura/resumo/observacoes) — `origem`/`prazoCliente`/`valorOrcado`/`orcamentoEnviado`/`ultimoContato`/`proximoContato`/`visitaOrcamentoData`/`visitaOrcamentoPeriodo` eram aceitos no tipo `Partial<Lead>` e passados por `AILeadModal.tsx` **e também por `LeadForm.tsx`** (criação manual), mas descartados silenciosamente ao salvar — sem esse fix, os campos que o Bug 2 passou a extrair da IA apareceriam certinho no preview do modal e sumiriam ao criar o lead. Corrigido acrescentando os mesmos nomes de coluna já validados em `updateLead` (`leadSlice.ts`) e `formatLead` (`formatters.ts`) ao payload do INSERT — mudança puramente aditiva, nenhum campo removido/renomeado. Nenhuma automação de `leadSlice.ts` (🔴) foi tocada, nem `useStore.ts`, `server.js` ou `/api/help-chat`. `tsc --noEmit`: 0 erros.
- **Fix v2.36.3 (app reiniciava do zero ao refocar aba):** bug reportado em produção — trocar de aba/janela e voltar fazia o app perder formulários em edição e redisparar toasts/notificações como num primeiro acesso. **Causa raiz** (`src/App.tsx`): o listener `supabase.auth.onAuthStateChange` chama `setSession(session)` para TODO evento, incluindo `TOKEN_REFRESHED` (reemitido pelo client Supabase ao refocar a aba, com um objeto `session` novo para o MESMO usuário). O `useEffect` de verificação de aceite de termos dependia do objeto `session` inteiro (`[session, checkTerms]`), então disparava de novo em cada refocus, chamando `setTermsStatus('checking')` de forma síncrona — e como App.tsx checa `termsStatus === 'checking'` antes de renderizar `MainLayout`, isso desmontava `MainLayout`/`ModuleRouter` inteiro (perda de todo `useState` local de formulários abertos) e remontava quando o check resolvia, reexecutando o efeito mount-only de `MainLayout.tsx` que chama `setupNotifications()` (notificação "resumo" redisparada). **Fix:** troca da dependência de `session` → `session?.user?.id` (primitivo, só muda em login/logout/troca real de usuário) em 2 efeitos de `App.tsx` — o de verificação de termos (fonte do bug) e o de bootstrap de workspace (`ensureWorkspaceForUser`+`initialize`+`startRealtime`, já protegido por `realtimeStarted.current` mas endurecido por consistência/clareza de intenção). Nenhuma outra linha mudou; `session` continua sendo lido normalmente dentro dos efeitos e em todo o resto do componente. Hipóteses descartadas por leitura direta do código: `vite:preloadError` (`main.tsx`, só dispara em falha real de import dinâmico, guardado por sessionStorage — não relacionado a refocus), `TermsGate.tsx` (sem lógica própria ligada à identidade de `session`), VitePWA `registerType:'autoUpdate'` (não força reload em refocus), store Zustand `initialize` (não zera arrays antes de repopular e nem é re-chamado em refocus — dados de leads/orçamentos nunca eram perdidos, só a árvore React). Novo padrão documentado em `_build/patterns.md` ("Dependência de efeitos ligados a auth: session vs. user.id") para evitar recorrência — já ocorreu 2x (bootstrap em jun/2026 mascarado pelo guard de ref, gate de termos em jul/2026 causando o bug visível). `tsc --noEmit` 0 erros. Só `src/App.tsx` tocado no código.
- **Feature (Central de Ajuda Fase 2, 17/07/2026):** rota `/api/help-chat` + painel de chat no frontend, sobre a base de `docs/help/*.md` da Fase 1. **Backend (`server.js`):** cache síncrona dos `.md` na subida (`helpDocsCache`, mesmo estilo de `fs.readFileSync` já usado pros logos, linhas ~228-266) — pasta ausente não derruba o boot, só loga warning e a rota responde 503; mapa `HELP_MODULE_TO_DOC` traduz os ids de `activeModule` pros nomes de arquivo onde divergem (`contas-receber`→`contas-a-receber`, `ia`→`ia-assistente`, `settings`→`configuracoes`). Rota nova `POST /api/help-chat` (`requireAuth`+`strictLimiter`, mesmo padrão de `/api/chat`) monta o `messages[]` no servidor — não é proxy cru: system prompt fixo (responder só com base na doc, admitir quando não sabe, nunca inventar feature) + doc do módulo ativo primeiro + demais docs em sequência (corpus inteiro, ~5-7k tokens, sem RAG — cabe folgado no contexto do `gpt-4o-mini`), histórico truncado a 6 itens no servidor mesmo que o cliente mande mais, `max_tokens:500`/`temperature:0.2`, resposta `{reply}`. **Frontend:** `src/lib/support.ts` (novo — `SUPPORT_WHATSAPP_NUMBER` **placeholder `'PREENCHER'` pendente de preenchimento manual antes do deploy** + `buildSupportWhatsappUrl`); `src/layout/HelpPanel.tsx` (novo — `SlidePanel` existente como casca, bolhas de mensagem réplicando o estilo de `MessageBubble` do WhatsApp sem importar do módulo, boas-vindas com 3 chips de sugestão, input Enter-envia/Shift+Enter-quebra igual ao WhatsApp, botão persistente "Falar com suporte" sempre visível abaixo do input); estado `helpOpen` vive em `MainLayout.tsx` (useState local, ao lado de `sidebarOpen`/`showUserMenu` — **não** tocou `useStore.ts`, área 🔴), passado como `onOpenHelp` pra `Sidebar` (novo item "Central de Ajuda" na seção Sistema) e `HeaderGlobal` (novo ícone `HelpCircle` 44×44 na área direita, entre Empresa e Sino) — os dois pontos de entrada abrem o mesmo painel, que lê `activeModule` direto do store no momento do envio (sem prop-drilling de contexto). `SlidePanel` fica sempre montado (só translada), então o histórico sobrevive a fechar/reabrir o painel na mesma sessão, sem persistir entre reloads (como pedido). Nada em `useStore.ts`, slices, `IAAssistente.tsx`/`iaService.ts`/`strategicDiagnosisService.ts` ou `/api/chat` foi tocado. `tsc --noEmit` 0 erros; `node --check server.js` ok. **Pendências antes do deploy:** (1) preencher `SUPPORT_WHATSAPP_NUMBER` em `src/lib/support.ts`; (2) deploy backend precisa levar `docs/help/*.md` pro working tree da VPS (lidos do disco em runtime) — ordem backend→frontend recomendada, não estritamente obrigatória (rota é aditiva).
- **Docs (Central de Ajuda Fase 1, 16/07/2026):** base de conhecimento de usuário criada em `docs/help/` — 16 arquivos .md (um por módulo + `primeiros-passos.md`), estrutura fixa (abertura = subtítulo do HeaderGlobal / "O que você pode fazer aqui" / "Passo a passo" / "Perguntas frequentes"), 43-54 linhas cada, linguagem de dono de negócio, zero jargão técnico. Fonte de autoridade: o código de `src/modules/*` (levantado por exploração) > subtítulos > FieldHints. Aberturas de `notas.md` e `fornecedores.md` adaptadas (subtítulos aprovados prometem NF-e integrada e "contatos e documentos" que não existem no código); pegadinhas documentadas: caixinha "PIX no PDF" (recibos), item valor 0 → observações no PDF (orçamentos), número manual DDI+DDD em conversas LID (whatsapp), link direto de logo e cor de destaque vazia (configurações). Bugs conhecidos NÃO documentados como feature (botão "Gerar Relatório" da IA morto, sino/Dashboard sem alerta de estoque por bug snake_case, campo Motivo de movimentação descartado) — listados como melhorias de UX na entrega. **Não commitado** (decisão da tarefa). Nenhum arquivo de código tocado.
- **UX v2.36.2 (subtítulos explicativos por módulo):** cada página ganha frase fixa abaixo do título, linguagem de dono de negócio (mesmo espírito dos FieldHints v2.36.1, nível de página). Ponto central único: `moduleSubtitles` (map módulo→texto) em `src/layout/HeaderGlobal.tsx`, renderizado como `<p>` abaixo da data — 1 arquivo tocado em vez de 16. Bônus/correção: `moduleNames` não tinha as chaves `recibos`/`whatsapp` (essas 2 páginas mostravam título genérico "CRM" no header) — adicionadas ("Recibos"/"WhatsApp") pra subtítulo fazer sentido embaixo do título certo. Header `h-16` (fixo) → `min-h-[64px]` (cresce pro subtítulo quebrar linha no mobile sem cortar sentido; 2 linhas aceitável, sem truncate/line-clamp). `admin` (Empresas) fica de fora do map — já tem h1+subtítulo próprios em `AdminEmpresas.tsx` (inline com botões "Atualizar"/"Nova Empresa", fora do padrão de header); só o texto do subtítulo foi trocado pro aprovado. `comparador` (módulo oculto, sem entrada de menu) ficou sem subtítulo — não fazia parte da lista aprovada. Nenhuma lógica/navegação alterada. `tsc --noEmit` 0 erros; `vite build` limpo (só aviso pré-existente de bundle >500KB).
- **UX v2.36.1 (tooltips explicativos em Configurações):** cada campo de `Settings.tsx` (abas Empresa, Documentos, Pagamentos) ganhou uma frase de ajuda em linguagem de dono de negócio (não de dev), sempre dizendo onde o valor aparece — meta é reduzir mensagem de suporte pra configuração inicial. Novo componente `src/components/ui/FieldHint.tsx` (wrapper `<p className="text-xs text-[var(--text-tertiary)] mt-1">`, reaproveita exatamente o padrão que já existia em alguns campos como Logo de fundo/Cor de destaque) — isolado em `Settings.tsx`, não tocou `Input.tsx`/`TextArea` (compartilhados por outros módulos). Aplicado nos ~19 campos que não tinham hint (Nome/CNPJ/Telefone/E-mail/Endereço/Logo Principal, Validade/Multiplicador, Observações padrão, PIX Tipo/Chave/Recebedor/Cidade em Documentos e Pagamentos) e os `<p>` de hint já existentes foram convertidos pro novo componente (mesma renderização, só consistência). Textos revisados em 2 rodadas: `_build/current-state.md`/plano não mantêm o texto completo — ver `src/modules/settings/Settings.tsx`. Aba Dados (ações/backup) manteve os textos existentes, só refinados "Exportar Backup" e "Importar Leads via CSV" (colunas esperadas do CSV explicitadas). Observação registrada (não implementada): label "Comissão da planejadora" é jargão de marcenaria, não se aplica à FL Art Metal/Ítalo — considerar renomear para algo genérico no futuro. `tsc --noEmit` 0 erros; `vite build` limpo (só o aviso pré-existente de bundle >500KB).
- **Feature v2.36.0 (LGPD — páginas públicas /privacidade e /termos + gate de aceite versionado):** conformidade LGPD mínima para operação SaaS, em 3 partes.
  - **(A) Fase 0 — textos jurídicos finalizados:** aplicadas TODAS as recomendações do parecer (`docs/legal/04-parecer-juridico.md.md`) aos docs `01-politica-de-privacidade.md` e `02-termos-de-uso.md` com redação literal, e resolvidos todos os `[PREENCHER]` (0 remanescentes): controlador = Gabriel Ferreira Costa / Vértice Digital / Uberaba-MG (sem CPF/CNPJ), Supabase = "AWS São Paulo, Brasil (sa-east-1)", Evolution/WhatsApp = "VPS própria (Alemanha, UE — Hetzner)", gateway = redação genérica "processadores de pagamento autorizados pelo Banco Central", dados de navegação = 6 meses, cookies de análise = "não utilizamos atualmente", DPO → "Encarregado pelo Tratamento de Dados", **prazo de suspensão por inadimplência = 15 dias** (decisão do Gabriel), foro Uberaba/MG, IA sem garantia de exatidão (6.1), cláusulas de estado da técnica + incidentes de segurança (11), backup "Recomenda-se que o usuário mantenha…", "no estado em que se encontra" → "disponibilizada conforme suas características… no momento da contratação". Removidas notas editoriais internas (blockquote "Versão revisada", "[Cláusula sensível]") e a referência ao nome de arquivo interno do DPA. `03-DPA` e `04-parecer` **não** viram páginas.
  - **(B) Páginas públicas:** rotas `/privacidade` e `/termos` servidas por branch de `window.location.pathname` em `src/main.tsx` (o app não tem router; navegação é 100% estado Zustand `activeModule`) — carregadas **lazy** (`src/modules/legal/PublicLegalRouter.tsx`, default export), fora do bundle principal. Renderizam o conteúdo FINAL dos `.md` via `react-markdown` + `remark-gfm` (tabelas GFM), importados `?raw` em `src/modules/legal/legalContent.ts` (**fonte única de verdade** — só 01/02 são importados; declaração `*.md?raw` em `vite-env.d.ts`). Shell `src/modules/legal/LegalPage.tsx`: header (logo VRTX + "Voltar"), prose `.legal-*` com tokens.css, tabelas com scroll horizontal (mobile-first), rodapé "Vigente desde 2026-07-15". Novo `vercel.json` (rewrite SPA `/(.*)` → `/index.html`) + `workbox.navigateFallback: 'index.html'` para deep-link em prod/PWA. Links discretos: CTA de login (Termos/Privacidade como `<a target=_blank>`) e footer da `AuthPage.tsx`.
  - **(C) Gate de aceite versionado (fail-closed):** `TERMS_VERSION='2026-07-15'` em `src/lib/terms.ts` (módulo compartilhado: cabeçalho dos `.md` + rodapé das páginas + coluna `version`). Migration `supabase/migrations/20260715000000_create_terms_acceptance.sql` (aplicada via MCP): tabela `terms_acceptance (id, user_id, version, accepted_at, UNIQUE(user_id, version))`, RLS `self_access` FOR ALL TO public USING/WITH CHECK `user_id = auth.uid()` (padrão copiado de `workspace_members`) — user-scoped (não workspace-scoped). `src/lib/termsAcceptance.ts`: `hasAcceptedCurrentTerms` (lança em erro → fail-closed) + `recordTermsAcceptance` (idempotente por `23505`, retorna `{error}`, nunca lança). Gate em `App.tsx`: estado `termsStatus: checking|accepted|needed|error` — o app (`MainLayout`/`ModuleRouter`) **nunca monta** enquanto ≠ `accepted`; `checking` → loader, `error` → tela "Tentar novamente" (não monta o app), `needed` → `src/components/TermsGate.tsx` (modal bloqueante de tela cheia, sem ESC/clique-fora, checkbox + links nova aba + Aceitar/Sair) que **só** chama `onAccepted` após INSERT confirmado no banco. Reseta para `checking` no logout. Vale para usuários existentes (FL/Ítalo veem uma vez).
  - **Verificação:** `tsc --noEmit` 0 erros; build limpo (só o aviso pré-existente de bundle >500KB; chunk `PublicLegalRouter` isolado ~176KB/54KB gzip — **react-markdown fora do bundle principal**). Isolamento de bundle confirmado por grep em `dist/assets`: `03-DPA`/`04-parecer` **ausentes** (0 ocorrências), texto de 01/02 só no chunk lazy. RLS testada via simulação de sessão real (`SET ROLE authenticated` + claims, rollback): usuário A vê só o próprio aceite (1), A **não** insere aceite p/ B (bloqueado por WITH CHECK), B **não** vê a linha de A (0); tabela deixada vazia. Páginas renderizadas via Puppeteer headless mobile (420px): 15/15 checks (h1 correto, 2 tabelas GFM em /privacidade, conteúdo Fase 0, "Vigente desde 2026-07-15", zero `[PREENCHER]`, sem notas internas, **sem erros de console/pageerror**). **Não** tocou em `useStore.ts`, `leadSlice.ts`, `server.js`, RLS existente nem no auth além do gate. `get_advisors(security)` antes/depois: só o aviso pré-existente `auth_leaked_password_protection`.
  - **Pendência de deploy:** frontend Vercel (novas deps `react-markdown`/`remark-gfm` + `vercel.json`). **Sem mudança no backend** (server.js intocado). Migration já aplicada no banco de produção via MCP.
- **Migração de dados 14/07/2026 (split FL Art Metal / Vértice Digital):** workspace `72b024c0-42e4-4113-8bb0-81ba6eb720bd` tinha `nome='Vértice Digital'` no banco mas continha, na prática, 100% dos dados e identidade da FL Art Metal (resquício de quando o CRM era single-tenant) — `workspace_settings` já trazia `empresa_nome="FL ART METAL"`, CNPJ, PIX, logos, etc. Migration `supabase/migrations/20260714000000_split_fl_artmetal_vertice_workspace.sql` (aplicada via MCP Supabase): **(1)** renomeou esse workspace para `nome='FL Art Metal'` e trocou `owner_id` pra `1350a291...` (conta flartmetal.uberaba@gmail.com); **(2)** criou workspace novo e vazio `001843dd-97fc-42a6-88ee-08c7a404903a` chamado "Vértice Digital" (`segment='saas'`, `owner_id` = host `dd50c052...`) para uso interno da agência — sem settings configuradas ainda, a fazer pela UI; **(3)** `workspace_members`: host (`dd50c052...`) rebaixado de `owner` pra `admin` no workspace FL (mantém suporte), e vira `owner` no workspace novo; `1350a291...` permanece `owner` do FL sem nenhuma mudança; **(4)** a instância WhatsApp `teste-gabriel` (número pessoal de teste do Gabriel, não da FL) foi movida do workspace FL pro workspace novo. **Nenhuma linha de leads/orçamentos/recibos/etc foi tocada** — auditoria prévia (Fase 0, somente leitura) confirmou 0 dados fora do padrão FL nas 14 tabelas multi-tenant, e a recontagem pós-migration bateu idêntica em todas as 13 tabelas restantes (só `whatsapp_instances` foi de 1→0 no workspace FL, esperado). `get_advisors(security)` antes/depois: só o aviso pré-existente não relacionado (`auth_leaked_password_protection`). Rollback documentado no próprio arquivo de migration (ids literais, sem placeholder).
- **Feature v2.35.0 (PDF v3 — redesign orçamento + recibo, cor por workspace):** reescrita visual dos templates **v2** de orçamento e recibo seguindo direção aprovada (header em card colorido, itens como **cards numerados**, resumo em tabela, caixa de total em evidência com **valor por extenso**), 100% derivado das settings do workspace, **sem migration** (novas keys key-value). **v1 (legado) intocado.**
  - **Novo token `cor_destaque`** (key `cor_destaque` em `workspace_settings`) + **novo campo `empresa_instagram`** (rodapé). Frontend: `settingsSlice.ts` (interface/defaults/mapa), `useDefaultSettings.ts`, `Settings.tsx` (aba Empresa: input Instagram + color-picker de destaque clonado do principal, com presets e botão "Usar cor principal", + preview com círculo numerado), 3 payloads (`Orcamentos.tsx` ×2, `Recibos.tsx` ×1). **Fallback:** `cor_destaque` vazia → usa a cor principal (helper `resolveCorDestaque` no backend — nunca quebra).
  - **Backend `server.js`:** helper `resolveCorDestaque` + helpers de template v3 reutilizados pelas 3 rotas (`fmtBRL`, `separarItens`, `maoDeObraItem`, `itemCardHtml`, `resumoTabelaHtml`, `caixaTotalHtml`, `observacoesHtml`, `footerContatoHtml`). Rotas v2 reescritas: `gerar-orcamento`, `gerar-orcamento-agrupado` (**full v3** por orçamento: cards+resumo+total, page-break por bloco), `gerar-recibo` (header alinhado à família, `th` em `cor_destaque`, valor por extenso repetido abaixo do total). `valorPorExtenso`/`dataExtenso` reutilizados. PIX, logos e marca d'água (v2.34.2) **intocados**.
  - **Itens de valor 0** deixam de ser descartados/exibidos como R$ 0,00 → migram para a seção **Observações** (orçamento e recibo v2). No recibo, `itens`/`obs` ficaram **version-aware** (v1 mantém todos os itens).
  - **Templates reescritos:** `templates/orcamento-v2.html` (o `<style>` é reaproveitado inline pela rota agrupada) e `templates/recibo-v2.html`.
  - **Verificação:** `node --check server.js` ok; `tsc --noEmit` 0 erros; 9/9 PDFs gerados HTTP 200 + inspeção visual (rasterização): 3 cenários de workspace (completo navy+laranja / só-principal verde monocromático / vazio → default laranja sem logo/marca), agrupado (2 orçamentos), recibo (extenso + obs migrada + PIX), e **regressão v1 legado confirmada intacta** (ainda mostra itens R$ 0,00 e o layout antigo).
  - **Pendência de deploy (ordem backend→frontend):** VPS `git pull && pm2 restart crm-backend` (server.js + templates) **antes** do frontend (Vercel). Campos novos são opcionais com default seguro (backward-compatible).

## Features Implementadas
- Financeiro com status (pendente/pago/vencido/a_vencer)
- Comissão da planejadora (% sobre totalBruto)
- Múltiplos orçamentos agrupados (PDF multi-orçamento)
- Template PDF profissional v2 (orçamento + recibo)
- Cores personalizáveis por workspace
- Recibo v2 + endereço no LeadDetail
- Notificações locais PWA (operacional)
- QR Code PIX nos PDFs + Copia e Cola + envio WhatsApp
- Assinatura digital (empresa + cliente, canvas + digitada)

## Design System
- Design System v2.25.1: botão "Registrar Contato" → variante secondary + ícone Phone verde (var(--success))
- Variante 'ia' adicionada no Button.tsx (roxo #a855f7 / hover #9333ea)
- Tokens atualizados: accent (#ff6a00 laranja), bg-app/sidebar/surface/surface-2/surface-3 ajustados
- Design System v2.26.0: tokens --ia + --ia-subtle adicionados; --border-accent corrigido para laranja; status badges de Recibos/Orçamentos/Financeiro/Operacional migrados para CSS vars; Settings reestruturada (4 tabs: Empresa, Documentos, Pagamentos, Dados); calendários DnD com inline styles; cores Tailwind hardcoded eliminadas em todos os módulos
- Design System v2.26.1: --bg-input definido em tokens.css (alias de --bg-surface-2); purple hardcoded zerado em IAAssistente.tsx (6 ocorrências) e LeadsList.tsx (2 ocorrências) — migrados para var(--ia) e var(--ia-subtle)
- Design System v2.26.2: hardcoded zerado em Notas.tsx (statusColors, statusIcons, stats, info banner, empty state), AuthPage.tsx (blue/gray → accent/text vars), Central.tsx (text-blue-600 × 2 → accent), OperacionalCalendar.tsx (bg-red-700 → danger)
- Design System v2.26.2 Lote A: LeadRow CSS vars (ring-blue/border-red/slate → accent/danger/text vars via inline style); Dashboard grid 2×2 mobile (Receita + Indicadores); rounded-2xl → rounded-xl em todos os Cards do Dashboard
- v2.26.3 Lote B: Painel Emergência Dashboard (leads críticos com 5+ dias sem contato, clicável → navega para lead); filtros Leads compactos mobile (busca + status na linha 1, temperatura + prioridade ocultos no mobile exceto quando filtro ativo)
- Design System v2.27.0: Bottom navigation mobile (5 abas: Início, Leads, Orçamentos, Agenda, IA), hambúrguer mantido para módulos extras (Kanban, Financeiro, Recibos, Settings)
- UX v2.27.1 Lote A: flash branco loader corrigido (index.html body+root #0f1117); touch targets header (botão sino + avatar → min 44×44px); placeholder busca leads encurtado + pl-9
- UX v2.27.2 Lote B: LeadDetail footer — Editar=primary/Excluir=ghost danger (hierarquia correta); Orçamentos — title+aria-label em Download/Editar/Excluir; OperacionalCalendar — dias vazios compactos (48px, fundo transparente, texto disabled)
- UX v2.27.3 Lote C: H1 duplicados removidos (Dashboard/Leads/Orçamentos/Settings); faixa mobile de alertas no MainLayout (abaixo do header, md:hidden); LeadRow — nome text-base, serviço text-xs disabled, badge inline semântico por nível, telefone tertiary, valor tabular-nums
- UX v2.27.4 Lote D: Orçamentos mobile — Editar/Excluir/Assinatura ocultos em mobile (hidden sm:flex), PDF+PIX sempre visíveis; Operacional — formulário Nova Tarefa colapsável (toggle com botão "+ Nova Tarefa"); Leads — "Criar com IA" rebaixado para ghost+color ia, texto abreviado em mobile
- UX v2.27.5 Lote E: Sidebar — item ativo com faixa accent (borderLeft 3px + accent-subtle bg + fontWeight 600) e section headers 10px/0.7 opacity; Dashboard — card Saúde CRM → faixa fina (~40px) com cor dinâmica por status (SAUDÁVEL/ATENÇÃO/CRÍTICO); Header — botão Sair e email removidos do header; avatar vira dropdown (nome + email + Sair) com overlay para fechar; avatar estático hardcoded removido do HeaderGlobal; Input/TextArea/Select — borda border-strong para melhor contraste visual
- UX v2.27.6 Lote F (final auditoria UX Cowork): StatCard — props suffix+valueStyle adicionadas (sem quebrar usos existentes); Dashboard Indicadores — Score Operacional com "/100", Tarefas Críticas com cor semântica (danger/success); Orçamentos — hierarquia invertida nas linhas: nome do cliente como título principal, numero ORC como texto secundário mono discreto; Settings grid já era cols-1/md:cols-2 (sem mudança necessária); botão menu mobile — aria-label adicionado
- Hotfix v2.27.7: LeadsList — Select de status envolto em wrapper shrink-0 w-[140px] (busca não colapsa mais); LeadRow — container flex-col/sm:flex-row responsivo (nome visível em 375px, valor+botões na linha 2 mobile); App.tsx — LoadingFallback e loader "Carregando dados..." migrados para fundo #0f1117 + spinner laranja (sem flash branco)
- Hotfix v2.27.8: Operacional — H1 "Painel Operacional" removido (duplicado com header); LeadRow badge "● Baixo" — color migrado de --text-disabled para --text-secondary (contraste WCAG AA); Dashboard Score Comercial — suffix " pts" adicionado (contexto de escala)
- UX v2.27.9: LeadRow — TempDot (dot 8px colorido por temperatura: danger/warning/info) substituiu TemperatureBadge; badge "✦ IA" (var(--ia)/var(--ia-subtle), 10px, rounded) substituiu "IA Analisado" rounded-full
- UX v2.28.0: Dashboard — Foco Hoje aprimorado (counter badge no header, TempDot 7px + alerta "Xd" por lead, min-h-44px); StatCard prop delta? ReactNode adicionada (Card.tsx); deltas em Receita Provável (Meta: R$X), Score Comercial (N fechados), Tarefas Críticas (N atrasadas)
- Fix v2.28.1: Badge.tsx — variant `info` corrigida (--info-subtle/--info azul, era accent laranja); variant `ia` adicionada (--ia-subtle/--ia roxo); StatusBadge `novo` → info, `atendimento` → ia (era bg-purple-100 hardcoded); PriorityBadge bullet ● adicionado, text-[10px] font-semibold
- Rebrand v2.28.9: AuthPage substituída por landing page SaaS VRTX (nav sticky, hero com mockup do dashboard, 6 feature cards, 3 steps, 3 depoimentos, CTA com botão Google + trust row, footer); fluxo OAuth `supabase.auth.signInWithOAuth` preservado intacto; CSS prefixado `lp-*` para isolar do design system interno
- Rebrand v2.28.8: favicon.ico adicionado ao index.html; title/description/apple-mobile-web-app-title → "VRTX CRM"; vite.config.ts PWA manifest name/short_name atualizados; Sidebar logo "CRM"→"VX", texto "CRM Pro"→"VRTX"
- Fix v2.28.7: LeadRow borderTop preservado — usa borderTopWidth/Style/Color separados + borderRight/Bottom/LeftColor para isSelected/alertaContato (borderColor shorthand não sobrescreve mais); sub-header mobile removido do MainLayout (redundante com HeaderGlobal); HeaderGlobal h1 text-sm→text-base + truncate, data hidden sm:block→truncate (visível mobile)
- UX v2.28.6: Orçamentos — botões PDF e PIX ganham label "PDF"/"PIX" visível apenas em mobile (sm:hidden, gap-1); IAAssistente — card onboarding (ia-subtle, border ia) exibido enquanto nenhum lead foi analisado, some após primeira análise
- UX v2.28.5: Empty states diferenciados — Leads (filtro ativo→🔍+limpar / vazio→👥+CTA), Orcamentos (📄+CTA), Recibos (🧾+contexto); Dashboard hierarquia h2 — Estoque Crítico/Visitas Hoje/Maiores Oportunidades rebaixados para text-sm uppercase secondary; Sidebar — badge danger (leads críticos) no Dashboard, badge count neutro (leads ativos) no Leads
- UX v2.28.4: Financeiro — card "Resultado do Mês" (receitas−despesas do filtro atual, cor semântica success/danger) no topo da lista; LeadsList — filtros Temperatura+Prioridade migrados de selects para chips horizontais com scroll universal (mobile+desktop), bloco mobile condicional removido
- UX v2.28.3: BottomNav — labels 9px→11px, height 64→72, ícone inativo #52525b, pill accent 16×3px no topo do item ativo; LeadRow — borderTop 3px colorida por prioridade, TempDot→pill emoji (🔥/☀️/❄️), PriorityBadge sem bullet, pill com border sutil rounded-[99px]; MainLayout — sub-bar mobile com nome do módulo abaixo do header
- UX v2.28.2: OrcamentoForm alinhado com design system — Multiplicador com pills atalho (Sem/+10%/+15%/+20%) + input customizado; seção ITENS com wrapper bordado, header uppercase e sub-header tabular (DESCRIÇÃO/QTD/UNIT/TOTAL, hidden mobile); label "CLIENTE" uppercase acima dos selects, grid responsive cols-1/sm:cols-2; TOTAL em accent laranja text-xl; botões finais full-width flex-1 min-h-[48px] com "Salvar Rascunho"/"Enviar" no modo criação

- Fix v2.34.3 (RLS multi-workspace): investigação do pedido "restaurar seletor de workspace no header" revelou que esse switcher **nunca existiu** (comentário `{/* workspace selector */}` em `HeaderGlobal.tsx` era só rótulo aspiracional; item "Empresas" na Sidebar é o painel admin `AdminEmpresas.tsx`, não troca de contexto). Antes de construir a UI, achado bloqueante via Supabase MCP: **12 das 14 tabelas multi-tenant** (`leads`, `orcamentos`, `recibos`, `transactions`, `notas`, `materiais`, `fornecedores`, `contas_receber`, `cotacoes_materiais`, `consumo_materiais`, `operacional_tasks`, `workspace_settings`) usavam RLS `workspace_id = (SELECT wm.workspace_id FROM workspace_members wm WHERE wm.user_id = auth.uid() LIMIT 1)` — `LIMIT 1` sem `ORDER BY` trava cada usuário num único workspace arbitrário, nunca no "ativo". Só o host (`dd50c052...`) tem 2+ memberships (workspaces `72b024c0` "FL ART METAL" e `9a9b96d5` "Ítalo Colares Drywall") — os demais clientes (FL dedicado, GPP, Teste) têm 1 membership só, então não eram afetados. Decisão do usuário: pausar a UI do switcher, corrigir primeiro o RLS. Fix em 2 fases: **(1)** auditoria de 76 chamadas `.from(...)` nas 12 tabelas (15 arquivos) — 47 já filtravam `workspace_id` explicitamente (INSERTs, SELECTs de boot, `Settings.tsx`), **29 não filtravam** (UPDATE/DELETE só por `.eq('id',...)`, mais 1 crítico: `reciboSlice.ts emitirRecibo` buscava `numero_recibo` do ano **sem filtro nenhum**, arriscando numeração cruzada entre workspaces do host) — todas as 29 ganharam `.eq('workspace_id', get().workspaceId)` (10 arquivos: `leadSlice`, `reciboSlice`, `cotacaoMaterialSlice`, `operacionalSlice`, `fornecedorSlice`, `contasReceberSlice`, `orcamentoSlice`, `notaSlice`, `financeiroSlice`, `materialSlice`), deployado **antes** da migration (sob RLS antigo esse filtro extra é no-op seguro). Realtime (`useStore.ts startRealtime`, 7 channels) auditado e já correto — `filter: workspace_id=eq.${workspaceId}` explícito por channel, `cleanupRealtime`/`setWorkspaceId` já resetam corretamente numa troca — nenhuma mudança necessária. **(2)** Migration `supabase/migrations/20260711000000_fix_rls_multi_workspace_membership.sql` — `DROP POLICY`+`CREATE POLICY` nas 12 tabelas trocando `= (...LIMIT 1)` por `IN (...)` (mesmo padrão já validado em `workspaces`/`whatsapp_instances`/`whatsapp_messages`), preservando exatamente os roles originais (`TO public` em 11, `TO authenticated` em `contas_receber`). Verificado via simulação de sessão RLS real (`SET ROLE authenticated` + `request.jwt.claims`, não bypass de superuser): host agora vê 35 leads (34+1, soma das duas memberships, antes só uma arbitrária); filtro explícito por workspace continua isolando certinho (34 e 1, não somado); acesso negado a workspace de outro cliente (GPP, 0 linhas); usuário de membership única sem nenhuma mudança (34, idêntico a antes). `get_advisors(security)` antes/depois: só o aviso pré-existente não relacionado (`auth_leaked_password_protection`). `tsc --noEmit`: 0 erros. **UI do seletor de workspace continua como tarefa futura separada** — este fix só resolve os dados; não há switcher no header ainda.
- Fix v2.34.2 (multi-tenant): removido o fallback estático da FL Art Metal (`logoPath`/`logoBgPath`, assets/logo_header.png e assets/logo_bg.png) das 3 rotas de PDF em `server.js` — `gerar-orcamento`, `gerar-orcamento-agrupado`, `gerar-recibo`, tanto v1 (legado) quanto v2, num total de 6 pontos. Antes: workspace sem `empresa_logo_url`/`empresa_logo_bg_url` configurado (ou download falho) fazia o PDF cair na logo/marca d'água da FL Art Metal — vazamento de marca entre clientes pagantes. Agora: ausência ou falha de download → PDF sem logo/sem marca d'água (nunca a de outro cliente). Dois helpers novos: `resolveWorkspaceLogo(rawUrl, logLabel, noun)` centraliza a lógica "sem URL/falha → '' + log" (logs `[PDF][logo_header]`/`[PDF][logo_bg]` mantidos, mensagens ajustadas de "caindo no fallback FL"/"usando fallback estático (FL Art Metal)" para "renderizando sem logo"/"renderizando sem marca d'água"); `injectLogoImg(html, placeholder, base64Value)` substitui `{{logo}}`/`{{logo_bg}}` quando há valor, ou remove a tag `<img>` inteira do HTML carregado em memória quando vazio (evita `src=""` quebrado) — nenhum arquivo `.html` em `templates/` foi alterado, só a string em runtime. `logoPath`/`logoBgPath` continuam existindo no arquivo (carregados de assets/) mas não são mais referenciados como fallback em nenhuma rota. Testado localmente as 12 combinações (3 rotas × v1/v2 × com/sem logo) + 1 caso de URL 404: todas HTTP 200, tamanhos de PDF consistentes (maior quando logo embutida em base64), logs corretos. Verificado via Supabase MCP: workspace FL Art Metal (`empresa_nome="FL ART METAL"`) já tem `empresa_logo_url`/`empresa_logo_bg_url` configurados no Storage — nenhuma ação necessária. **Pendência**: workspace GPP Móveis Planejados não tem nenhuma das duas chaves em `workspace_settings` — PDFs da GPP agora saem sem logo/marca d'água até alguém configurar (antes esse buraco estava mascarado pela logo da FL vazando). `node --check server.js`: sem erro de sintaxe.
- Fix v2.34.1: Login quebrava (406 + logout automático) para usuário com 2+ memberships em `workspace_members`. `ensureWorkspaceForUser` (`src/lib/supabaseWorkspace.ts`) filtrava só por `user_id` com `.single()` — PostgREST retorna 406 quando há 2+ linhas, tratado como "sem workspace", forçando `signOut()` em `App.tsx`. Corrigido: query sem `.single()` busca todas as memberships (`order('created_at', asc)`); 1 workspace → usa ele; 2+ → usa o salvo em `localStorage['vrtx_active_workspace_id']` se ainda válido, senão o mais antigo, persistindo a escolha. 0 workspaces → mesmo comportamento de erro de antes. Não existia switcher de empresa no header nem persistência prévia de workspace ativo (div estática só exibe nome) — criada a chave de localStorage como base mínima para isso. `useIsAdmin.ts` (também usa `.single()` em `workspace_members`, mas filtro composto `workspace_id`+`user_id`) e `AdminEmpresas.tsx` (usa rota backend própria, não Supabase client) não precisaram de mudança. tsc: 0 erros.
- Fix v2.34.0: Tela branca na 1ª navegação pós-deploy corrigida em 2 camadas. **Causa raiz:** VitePWA `registerType:autoUpdate` com `globPatterns:'**/*.js'` apaga chunks antigos ao ativar novo SW; aba já aberta tem `index.html` antigo → `import()` de hash obsoleto rejeita → sem Error Boundary, React desmonta a árvore → branco (F5 resolve pq baixa `index.html` novo). **Camada 1 (`src/main.tsx`):** listener `vite:preloadError` (evento emitido pelo Vite exatamente nessa falha) faz `window.location.reload()` UMA vez; guarda `sessionStorage('vrtx-chunk-reloaded')` previne loop (sessionStorage persiste no reload mas é limpo ao fechar a aba). **Camada 2 (`src/components/ErrorBoundary.tsx` novo + `src/App.tsx`):** Error Boundary wrapping `<ModuleRouter/>` — se um erro de render/import escapar da Camada 1, exibe UI "Não foi possível carregar o módulo + botão Recarregar (≥44px, tokens.css)" em vez de branco; `componentDidCatch` loga o erro real no console. Antes: zero Error Boundary no projeto. tsc: 0 erros. Build: ✓ 6.02s.
- UX v2.33.9: Contas a Receber — observação exibida no card da lista. Linha `<p class="text-xs text-[var(--text-secondary)] truncate mt-0.5">` renderiza condicionalmente (`conta.observacao && ...`) entre descrição e metadados (vencimento/forma/lead). Contas sem observação: sem linha extra. Truncate preserva densidade compacta mobile. Só `ContasReceber.tsx` tocado.
- Fix v2.33.8: Contas a Receber — falha silenciosa no UPDATE corrigida em dois níveis. (1) `contasReceberSlice.ts`: `addToast?.` → `addToast` (sem optional chaining) em ambos `addContaReceber` e `updateContaReceber` — erros Supabase agora sempre exibem toast. (2) `ContasReceber.tsx handleSubmit`: `onClose()` passou a ser chamado incondicionalmente mesmo quando o update falhava (retornava `undefined`) — corrigido com `const result = await updateContaReceber(...); if (!result) return;` (idem para `addContaReceber`). Comportamento anterior: modal fechava na animação mascarando o toast de erro, dando falsa impressão de sucesso. Agora: falha mantém modal aberto + toast visível. MCP confirmou que SQL UPDATE direto funciona (RLS OK como superuser); constraints da tabela: CHECK `forma_recebimento` (pix/boleto/etc), CHECK `status` (pendente/recebido/atrasado/cancelado) — ambos aceitam NULL (padrão PostgreSQL: CHECK com NULL → não-violado). Nenhum dado existente corrompido: todos os 6 registros têm `updated_at = created_at` (nunca editados), conteúdo original de descricao/observacao intacto no DB.
- Fix v2.33.6: WhatsApp LID — indicador visual de "número sugerido" no input manual. Estado `suggestedJids` (Record `jid→boolean`) em `WhatsApp.tsx` marca quando o campo foi auto-preenchido pelo `contactsMap` (no `useEffect` de auto-fill, dentro do ramo "campo vazio"). Banner LID agora: (1) troca o texto para "Número sugerido a partir dos seus contatos. Confira antes de enviar." + chip "sugerido" (`var(--info)`/`var(--accent-foreground)`); (2) borda do input em `var(--info)`; (3) botão "Limpar" (Trash2, ≥44px) que zera o campo. Ao usuário digitar (`onChange`) a flag cai → vira "digitado" (texto/chip/borda voltam ao normal). Regras de segurança preservadas: sem write de `phone_number`, sem auto-resolve, sem auto-envio, pushName ambíguo continua sem sugerir. Só `WhatsApp.tsx` tocado; zero erro TS novo.
- Fix v2.33.5: WhatsApp — auto-preencher campo de número para conversas LID via contactsMap. `fetchContacts` agora detecta pushNames duplicados e os exclui do mapa (pushName ambíguo → campo fica vazio → usuário digita). `useEffect` de auto-fill em `WhatsApp.tsx` estendido para usar `resolvedNumber` (contactsMap) como fallback após `savedPhone` (envio anterior). Header já exibia o número corretamente; agora o input também é preenchido automaticamente.
- Fase 12.7 v2.33.0: WhatsApp — salvar número digitado (LID) + mídia inline (imagem/áudio). (1) server.js `/api/whatsapp/send-text`: após upsert do envio, se `originalJid` é `@lid`, `UPDATE whatsapp_messages SET phone_number=sendNumber WHERE remote_jid=originalJid AND workspace_id` (número autoritativo = digitado pelo usuário, sem filtro IS NULL) + log `Saved mapping`. (2) Nova rota `GET /api/whatsapp/media/:instanceName/:messageId` (proxy Evolution `POST /chat/getBase64FromMediaMessage`, `evolutionConfig`+`requireAuth`, retorna `{base64,mimetype}` ou 404). (3) `formatWhatsappMessage` reexpõe `phoneNumber: raw.phone_number ?? null`; tipo `WhatsappMessage.phoneNumber?: string|null`. (4) `WhatsApp.tsx`: componentes `WhatsAppImage` (≤280×300, `var(--radius-md)`, clique→nova aba, caption≠'[Imagem]') e `WhatsAppAudio` (player nativo ≤260px), lazy-load por mount via `apiFetch`, com estados Carregando/indisponível em `var(--text-tertiary)`; `MessageBubble` recebe prop `instanceName` e ramo de mídia para `image`/`audio` (fallback ícone+label p/ video/document/sem messageId); **auto-fill seguro** — `savedPhone` lido SÓ de `messages[].phoneNumber` (escopado à conversa via fetchMessages remote_jid), nunca de `whatsappContacts` (fonte da contaminação revertida na v2.32.7); `useEffect` preenche `manualNumberByJid` se vazio; header LID prioriza `savedPhone` → `resolvedNumber` (contatos, só display) → "Número não identificado"
- Fix v2.32.7: WhatsApp envio — rollback defensivo de auto-preenchimento. Após reports de mensagens enviadas pra contato errado em conversas LID, removido **todo** auto-fill do input manual. (1) server.js `/api/whatsapp/send-text`: removido `phone_number: sendNumber` do upsert e bloco `UPDATE ... WHERE remote_jid=originalJid AND phone_number IS NULL` (propagação LID). Persistência continua com `remote_jid = originalJid || data.key.remoteJid` (unificação mantida da v2.32.6). (2) `WhatsApp.tsx`: removido `knownPhoneFromMessages` da derivação de `resolvedNumber` e `useEffect` que injetava `manualNumberByJid` automaticamente. Input manual em LID agora começa SEMPRE vazio; usuário digita todo envio. (3) `formatWhatsappMessage` e tipo `WhatsappMessage`: campo `phoneNumber` removido (coluna `phone_number` no Supabase permanece mas não é lida/escrita pelo código). (4) `whatsappContacts`/`fetchContacts` mantidos só para display do header (sem efeito colateral). Resultado: zero risco de cruzamento por colisão de pushName ou contaminação entre LIDs; thread continua unificado via `originalJid`
- Fix v2.32.6: WhatsApp — virada arquitetural LID. `remote_jid` deixou de ser trocado; número real vai na coluna `phone_number` (já existia em `whatsapp_messages text NULLABLE`). (1) Webhook `/webhook/evolution`: bloco de resolução `@lid → @s.whatsapp.net` da v2.32.4 **removido**; grava sempre `message.key.remoteJid` original. (2) Rota `/api/whatsapp/send-text` agora aceita `originalJid` no body — persiste mensagem enviada com `remote_jid = originalJid || data.key.remoteJid` e `phone_number = sendNumber`; quando `originalJid` contém `@lid`, propaga `phone_number` para mensagens antigas dessa conversa com `phone_number IS NULL` (`UPDATE whatsapp_messages WHERE remote_jid=originalJid AND phone_number IS NULL`); log `[WhatsApp] Mapped LID xxx → yyy`. (3) `whatsappSlice.sendMessage` envia `originalJid: selectedConversation` no body. (4) `formatWhatsappMessage` ganhou `phoneNumber: raw.phone_number ?? null`; tipo `WhatsappMessage.phoneNumber: string | null`. (5) `WhatsApp.tsx`: `resolvedNumber` agora prioriza `messages.find(m => m.phoneNumber)?.phoneNumber` (fonte canônica) e cai pra `whatsappContacts[contactName]` como fallback (v2.32.5 ainda útil quando ainda não houve envio). Resultado: uma única conversa por contato (LID ou não); envios+recebimentos no mesmo thread; input manual auto-preenchido após primeiro envio
- Fix v2.32.5: WhatsApp — número real de contatos LID + quoted messages. (1) Nova rota `GET /api/whatsapp/contacts/:instanceName` em server.js (após `/logout/:inst`) — proxy Evolution `POST /chat/findContacts`, filtra contatos `@s.whatsapp.net`, retorna `{pushName,remoteJid,number,profilePicUrl}`. (2) Slice ganhou `whatsappContacts` (Record `pushName→number`) + `fetchContacts` (indexa por pushName normalizado lowercase/trim). (3) `WhatsApp.tsx`: `fetchContacts` no mount junto de `fetchConnectionStatus`; `resolvedNumber` cruzado por `contactName`; header do chat mostra `formatPhone(resolvedNumber)` para conversas LID (ou "Número não identificado"), `jidToPhone` mantido p/ DM normal; `useEffect` pré-preenche `manualNumberByJid` com número resolvido quando usuário ainda não digitou; `MessageBubble` ganhou `getQuotedText` (extrai `rawData.message.{extendedTextMessage,imageMessage}.contextInfo.quotedMessage`) + barra de citação (borderLeft accent, `--bg-surface-2`, truncada 80 chars). Tipos em useStore.ts atualizados
- Fix v2.32.4: WhatsApp — unificação LID + persistência de envios. (1) Webhook `/webhook/evolution` agora resolve `@lid → @s.whatsapp.net` via Evolution `/chat/findContacts/{instance}` antes de upsert no Supabase: tenta `where: { remoteJid }` (LID exato) e fallback `where: { pushName }`; pega primeiro contato com sufixo `@s.whatsapp.net`. Falha de rede/JSON/contato não-encontrado → mantém LID original (graceful degradation, log claro `LID xxx resolved to yyy` ou `not resolved — keeping original`). (2) Rota `/api/whatsapp/send-text`: após `response.ok` com `data.key.{id,remoteJid}`, persiste mensagem enviada via `getSupabaseAdmin().from('whatsapp_messages').upsert(...)` com `from_me=true`, `message_id=data.key.id`, `onConflict: 'message_id'` (de-dup contra eco do webhook). Resultado: envios e recebimentos da mesma pessoa ficam no mesmo `remote_jid` canônico; Realtime (já correto em useStore.ts:545-589) reflete as duas direções ao vivo. Verificado via MCP: `whatsapp_messages` está em `supabase_realtime` publication, `replica identity=d` (ok p/ INSERT)
- Fix v2.32.3: WhatsApp envio — tratamento de LID e grupos. server.js `/api/whatsapp/send-text` agora rejeita `@g.us` (400 `groupNotSupported`) e `@lid` (400 `lidDetected`) antes de chamar Evolution, e normaliza demais formatos (`@s.whatsapp.net` ou cru) para apenas dígitos. `whatsappSlice.sendMessage(text, overrideNumber?)` ganhou segundo parâmetro — frontend passa número manual quando conversa é LID; guarda client-side bloqueia grupo + LID sem número. `WhatsApp.tsx`: helper `isLid`, state local `manualNumberByJid` (Record), banner amarelo (`--warning-subtle`/`--warning`) em grupos com textarea desabilitada; banner azul (`--info-subtle`/`--info`) em LID com input `type=tel inputMode=numeric` para DDI+DDD+telefone; botão Send desabilitado até `lidNumberValid` (≥10 dígitos). Tipo `sendMessage` em useStore.ts atualizado com `overrideNumber?: string`
- Fix v2.32.2: WhatsApp send-text — `sendMessage` agora envia `remoteJid` completo (com sufixo `@s.whatsapp.net` ou `@lid`) no campo `number` em vez do número extraído. Suporte a JIDs LID (ex: `258815266181294@lid`) que não são números de telefone reais — `jidToNumber` quebrava esses casos. Evolution API aceita JID direto. server.js `/api/whatsapp/send-text` passa `number` sem modificar (já estava ok)
- Fix v2.32.1: três bugs corrigidos — (1) WhatsApp send-text: `jidToNumber` agora extrai apenas dígitos (`.replace(/\D/g,'')`) — protege contra jids de grupo e sufixos `@s.whatsapp.net`; `sendMessage` loga corpo de erro real da Evolution + toast com status HTTP; toast dedicado quando `whatsappInstanceName` nulo (antes era return silencioso); (2) Contas a Receber — `addContaReceber` e `updateContaReceber` agora exibem toast com `error.message` do Supabase em vez de só `console.error` (insert/update falhavam em silêncio); (3) Comparador de Preços — `ComparadorPrecos.tsx` deriva `materialNome` (lookup em `materiais` por id) e passa como `material` para o slice (coluna `material` em `cotacoes_materiais` provável NOT NULL); slice inclui `created_at` no insert + toast com `error.message`
- Fix v2.29.2: server.js — `urlToBase64` helper adicionado; `empresa_logo_url` convertida para base64 antes de inserir no HTML em orçamento simples, agrupado e recibo v2 (logo quebrada no Puppeteer VPS corrigida)
- Fix v2.29.1: Score Operacional — suffix corrigido de "/100" → " pts" (escala é penalidade ilimitada, não 0-100); IAAssistente — data `dataIdealFollowUp` formatada com `toLocaleString('pt-BR')` em vez de ISO bruto; botão "Analisar Lead" — wrapper `position:relative; z-index:10` adicionado para garantir clicabilidade

## Fixes e Auditorias
- RLS Supabase: 14 tabelas, 1 policy filtrada cada
- Auditoria Codex: 6/6 bugs corrigidos
- Sanitização HTML completa nos PDFs (escapeHtml)
- Realtime cleanup no logout/troca workspace
- Logs sem PII nas rotas admin
- Idempotência básica (orcamentos + recibos)
- PDFs compactos (seções vazias colapsam)
- Fix: try/catch com fallback na busca sequencial do orçamento (server.js) — PDF gerado mesmo com Supabase inacessível
- Segurança v2.29.0 — hardening pré-VPS aplicado: (1) `VITE_OPENAI_API_KEY` removida do frontend (chave deixou de ir no bundle JS público); env backend renomeada para `OPENAI_API_KEY`; (2) rotas `/api/admin/*` protegidas por `requireAuth` (Supabase JWT via Bearer) + `requireAdmin` (whitelist `ADMIN_USER_IDS` env, fail-closed); (3) CORS manual substituído pelo pacote `cors` com whitelist via `CORS_ALLOWED_ORIGINS` (default localhost+Vercel); (4) `helmet` adicionado (CSP/COEP off por conta dos PDFs inline); (5) `express-rate-limit` adicionado — globalLimiter 100 req/min, strictLimiter 10 req/min em `/api/chat`, `/api/gerar-orcamento`, `/api/gerar-orcamento-agrupado`, `/api/gerar-recibo`, `/api/admin/criar-empresa`; (6) `express.json({ limit: '10mb' })` + `urlencoded` com mesmo limit

## Clientes Ativos
- FL Art Metal (metalúrgica) — ativo, usando diariamente
- GPP Móveis Planejados (marcenaria) — CRM em implantação
- Ítalo Colares Drywall — onboarding tráfego (sem CRM)

## Infraestrutura
- Frontend: Vercel (vertice-digital-crm.vercel.app)
- Backend: produção na VPS Hetzner (178.104.236.222), `/opt/crm-backend`, gerenciado via PM2 (processo `crm-backend`), Nginx reverse proxy 80→3001, API pública `https://api.vrtxcrm.com.br`. Deploy: SSH na VPS → `git pull origin main && pm2 restart crm-backend`. Dev local continua em `localhost:3001` (ngrok não é mais necessário)
- Supabase: online, RLS 100%
- VPS `.env`: `NODE_ENV=production` adicionado manualmente em 23/07/2026 — não versionado (`.env` gitignored), necessário para o CORS por ambiente do fix de segurança (localhost só entra na whitelist fora de produção). `CORS_ALLOWED_ORIGINS` deve conter apenas domínio(s) real(is) em prod.

## WhatsApp / Evolution API
- Fase 12 concluída: webhook `/webhook/evolution` no server.js (POST, recebe `messages.upsert`)
- `whatsapp_instances` criada no Supabase (FK → workspaces, RLS, `instance_name UNIQUE`)
- `whatsapp_messages` já existia no Supabase (RLS ativa)
- Mapeamento instance_name → workspace_id via tabela `whatsapp_instances`
- Instância não mapeada em `whatsapp_instances` → mensagem REJEITADA no webhook (`no_workspace` + warning). Fallback `DEFAULT_WORKSPACE_ID` REMOVIDO no fix de segurança 07/2026 (era vetor de mistura cross-tenant); a env pode ser removida do `.env` da VPS (não tem mais efeito).
- Rotas proxy `/api/whatsapp/*` validam ownership da instância (`assertInstanceOwnership`: instância pertence a workspace do usuário via `workspace_members`) antes de repassar ao Evolution — fix IDOR cross-tenant 07/2026. Falha → 403.
- Evolution API: https://evo.vrtxcrm.com.br
- Fase 12.5 concluída (v2.32.0): WhatsApp funcional — `apiFetch` agora anexa `Authorization: Bearer <jwt>` via `supabase.auth.getSession()` (cache local; conserta gap latente do `/api/admin/*`, não-quebra rotas sem auth); 4 rotas proxy Evolution em server.js após `/webhook/evolution` (`POST /api/whatsapp/send-text|send-media`, `GET /api/whatsapp/status/:inst`, `POST /api/whatsapp/logout/:inst`, todas `requireAuth` + globalLimiter, helper `evolutionConfig` valida envs); slice ganhou `whatsappInstanceName`/`connectionStatus`/`sendingMessage` + `fetchWhatsappInstance` (lê `whatsapp_instances` por workspace), `fetchConnectionStatus`, `sendMessage` (sem insert otimista — chega via webhook→realtime), `deleteConversation` (delete Supabase + update local, RLS); UI: footer fixo com textarea (Enter envia / Shift+Enter quebra) + botão Send, menu ⋮ com status (bolinha success/danger) e "Limpar conversa" (ConfirmDialog). ENV backend necessária: `EVOLUTION_API_URL=https://evo.vrtxcrm.com.br`, `EVOLUTION_API_KEY=<key>` (adicionar manual no .env do VPS)
- Auditoria UX WhatsApp v2.31.1: 3 fixes — `color:'#fff'` (×2 avatares) → `var(--accent-foreground)`; `focus:ring-2` → `focus:ring-2 focus:ring-[var(--accent)]` no input de busca; zero CSS hardcoded restante
- Fase 12 Passo 6 concluída: módulo WhatsApp (somente leitura) no frontend — `createWhatsappSlice` (fetchConversations agrupa por remote_jid no cliente, fetchMessages, setSelectedConversation), `formatWhatsappMessage` em formatters.ts, tipos `WhatsappMessage`/`WhatsappConversation`; página split-panel `src/modules/whatsapp/WhatsApp.tsx` (lista 30% + chat 70% desktop / fullscreen mobile com voltar, busca, bolhas por from_me, tipos não-texto com ícone+label, auto-scroll); canal realtime `realtime-whatsapp-messages` (INSERT) em useStore.startRealtime() append-only; item "WhatsApp" na sidebar (seção Comercial, ícone MessageCircle); rota `whatsapp` no ModuleRouter. BottomNav inalterada (5 itens, máximo)

- UX v2.33.4: Topbar — NotificationsDropdown no sino + remoção de badges redundantes. Novo `src/layout/NotificationsDropdown.tsx` (self-contained, lê leads/materiais/contasReceber/stats do store): badge com soma de 4 categorias ("99+" quando >99), dropdown `position:absolute right:0 zIndex:200` com `@keyframes fadeSlideDown 0.15s` (adicionado em index.css), click-outside via useRef+mousedown listener. 4 categorias clicáveis→navegam e fecham: leads críticos (AlertCircle/danger→'leads'), contas atrasadas (DollarSign/warning→'contas-receber'), tarefas pendentes (CheckSquare/accent→'operacional'), estoque baixo (Package/info→'estoque'); count===0 fica apagado (opacity .5); empty state "Tudo em dia". `HeaderGlobal.tsx`: removidos badges "X atrasadas/X hoje" + botão sino estático → `<NotificationsDropdown/>`, sino agora visível em mobile+desktop, ordem direita = Empresa→Sino→Avatar com gap 10px. `MainLayout.tsx`: removida sub-barra mobile de badges (stats/useDashboardStats limpos), gap-2.5 no container do header. Bônus: corrigido `var(--error)` (token inexistente) → `var(--danger)` em Kanban.tsx:302 (regressão da v2.33.1). Type-check dos arquivos tocados limpo
- UX v2.33.3: Dashboard — Saúde do CRM expandível (toggle click). Faixa mantida idêntica no estado fechado; ChevronDown aparece só quando há reasons; animação grid-template-rows 0fr→1fr 0.25s; novo componente `CrmHealthReasons.tsx` (Clock/DollarSign/FileText/TrendingDown por tipo, cor semântica crit→danger/warn→warning, botão ação accent-subtle); `criticos` extraído para useMemo compartilhado (Emergência + Saúde); `orcamentos` adicionado ao useStore do Dashboard; `'#fff'` no badge substituído por `var(--accent-foreground)`; máximo 4 reasons: leads críticos, contas atrasadas, orçamentos vencidos (validadeEmDias > 0), receita abaixo de 50% da meta
- UX v2.33.2: Dashboard — empty state "Sem projeções ainda" no card Previsão de Receita quando receitaPotencial + receitaProvavel + receitaConservadora === 0: ícone TrendingUp 56×56 accent-subtle, texto explicativo, botão "Ver leads quentes" → setActiveModule('leads') (1px dashed border-hover, accent-subtle bg); card "Receita Prevista (Contas)" sempre visível abaixo do empty state; quando há dados, grid 2×4 normal mantido intacto
- UX v2.33.1: Kanban — indicador "Xd sem contato" no rodapé de cada card. `daysSinceContact` + `contactSeverity` adicionados em `src/utils/date.ts`; `SortableLeadCard` em `Kanban.tsx` exibe ícone Clock 12px + texto 11.5px com cor semântica (≤1d: oculto, 2-4d: --text-tertiary, 5-6d: --warning, ≥7d: --danger) + borda colorida no card via inline style (--warning/--danger, só quando não há followUpAtrasado sobreposto)

## Próximas Prioridades
1. Adicionar `EVOLUTION_API_URL` + `EVOLUTION_API_KEY` no `.env` do backend (VPS) — confirmar se já configurado pós-migração
2. Fases 14-15 Evolution API (envio de mídia na UI, templates)

## Concluído (infraestrutura)
- ✅ Migração backend para VPS Hetzner (178.104.236.222) — PM2 (`crm-backend`) + Nginx reverse proxy, API pública `https://api.vrtxcrm.com.br` (supera decisão anterior "backend local via ngrok")

## Pendências Manuais
- Deletar nota órfã id:58118722 na tabela `notas` (dado de teste, workspace Vértice Digital) — fazer via Supabase Dashboard

## Módulos Ocultos (código intacto, sem entrada de menu)
- **Comparador de Preços** (`src/modules/suprimentos/ComparadorPrecos.tsx`): item comentado em `Sidebar.tsx:59`. Import lazy em `App.tsx` e entrada no `ModuleRouter` mantidos. Reativar: descomentar a linha em Sidebar.tsx.

## Dívida Técnica Conhecida
- Bundle ~884KB
- Offline/IndexedDB não implementado
- Cálculo de orçamento duplicado em 3 locais (domain/, slice, componente)
